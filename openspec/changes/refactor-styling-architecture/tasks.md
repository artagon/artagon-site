## 1. Implementation

- [ ] 1.1 Create `src/components/ui/` directory.
- [ ] 1.2 Identify reusable styles in `src/pages/vision/index.astro` matching `theme.css`.
- [ ] 1.3 Extract generic "Card" styles into a `Card.astro` component or global `.card` utility refinement.
- [ ] 1.4 Extract "Section Header" pattern (H2 + Number) into a reusable component.
- [ ] 1.5 Move unique but reusable page sections (like "Hero" gradients) to `theme.css` if they are to be standard.
- [ ] 1.6 Refactor `src/pages/vision/index.astro` to remove `<style>` block and use new components/classes.
- [ ] 1.7 Verify visual regression (ensure the page looks the same).
