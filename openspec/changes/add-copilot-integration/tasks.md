## 1. Implementation

- [ ] 1.1 Add `.github/workflows/copilot-setup-steps.yml` with a `copilot-setup-steps` job that installs Node 20 and runs `npm ci`.
- [ ] 1.2 Document the Copilot setup workflow location and purpose if needed.
- [ ] 1.3 Validate with `openspec validate add-copilot-integration --strict`.
