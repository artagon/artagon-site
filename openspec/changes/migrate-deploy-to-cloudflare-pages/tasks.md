## 0. Phase 0 — Pre-flight (Cloudflare account setup)

- [ ] 0.1 Run `openspec validate --strict migrate-deploy-to-cloudflare-pages`. Acceptance: validation reports valid.
- [ ] 0.2 Create a **DEDICATED** Cloudflare account that will own ONLY the `artagon` Pages project. Document the account ID in a secure password manager (NOT committed to repo). Per multi-reviewer-r1 finding [CR-002] the account is the security boundary, not per-project token scoping. Files: none committed. Acceptance: account ID recorded; `wrangler whoami` shows the dedicated account; account contains zero other projects.
- [ ] 0.3 Generate a Cloudflare API token with `Account.Cloudflare Pages:Edit` filtered to the dedicated account. Document token rotation policy (quarterly). Files: none committed. Acceptance: token validates against Cloudflare's API; `gh secret set CLOUDFLARE_API_TOKEN` succeeds; account-resource filter shows the single dedicated account ID.
- [ ] 0.4 Add `secrets.CLOUDFLARE_API_TOKEN` and `secrets.CLOUDFLARE_ACCOUNT_ID` to GitHub repo secrets via `gh secret set`. Files: none committed. Acceptance: `gh secret list` shows both.
- [ ] 0.5 Create the Cloudflare Pages project: `wrangler pages project create artagon`. Disable Auto-Minify in project settings. Files: none committed. Acceptance: `wrangler pages project list` shows `artagon` project.
- [ ] 0.6 Create a quarterly token-rotation reminder workflow at `.github/workflows/cloudflare-token-rotation-reminder.yml`. Trigger: cron `0 0 1 */3 *` (1st of every 3rd month). Action: opens a GitHub issue with title `[CHORE] Rotate Cloudflare API token (quarterly)` + checklist body. Files: new workflow. Acceptance: workflow YAML lints clean; manual `workflow_dispatch` opens a real issue. Per multi-reviewer-r1 finding [M-6] (Dependabot doesn't rotate API tokens; this workflow does the reminder).

## 1. Phase 1 — Build the headers + redirects pipeline

- [ ] 1.1 Extend `scripts/csp.mjs` to ALSO emit `dist/_headers` (NOT `public/_headers`) alongside the existing `<meta>`-tag injection. The script writes `dist/_headers` directly to avoid the public→dist copy-ordering trap (Astro copies `public/` to `dist/` BEFORE postbuild runs). Per multi-reviewer-r1 finding [CR-001]. Files: `scripts/csp.mjs`, `dist/_headers` (generated, gitignored). Acceptance: `node scripts/csp.mjs` produces `dist/_headers` with the global `/*` block + per-class blocks; `git ls-files dist/_headers public/_headers` returns empty.
- [ ] 1.2 Build `scripts/verify-headers.mjs`: parse `dist/_headers`, verify the global `/*` CSP is a SUPERSET of every per-page `<meta>` CSP (header-only directives `frame-ancestors` / `report-to` / `report-uri` / `upgrade-insecure-requests` are ALLOWED to differ — maintain explicit allowlist), verify presence of the 7 required security headers, verify HSTS `max-age` matches the documented stage from `docs/hsts-stage.md`. Exit codes 0/1/2 per the existing verify-script convention. Files: `scripts/verify-headers.mjs`. Acceptance: against the Phase 1.1 output, gate exits 0; manually mutating `_headers` to drift CSP flips to non-zero with the offending route + directive.
- [ ] 1.3 Add `node:test` tests for both: `tests/csp-headers-emission.test.mjs` (verifies `csp.mjs` writes `dist/_headers` correctly), `tests/verify-headers.test.mjs` (verifies the gate). Per multi-reviewer-r1 finding [M-1] uses flat `tests/*.test.mjs` layout. Files: 2 new test files. Acceptance: ≥ 12 tests pass covering happy path + each violation type + header-only-directive allowlist.
- [ ] 1.4 Add npm scripts: `verify:headers` → `node scripts/verify-headers.mjs`. Files: `package.json`. Acceptance: `npm run verify:headers` invocable.
- [ ] 1.5 Wire `verify:headers` into `postbuild` after `csp.mjs`. **Coordination with PR #44** (self-host-woff2-fonts): both PRs add postbuild gates. Whichever PR merges 2nd MUST rebase the chain; full target chain (assuming both land): `verify:prerequisites && verify:design-prerequisites && lint:tokens && verify-font-self-hosting && verify:font-metrics && measure:font-payload && sri && csp && verify:headers && lint:design && lint:design-md-uniqueness`. Per multi-reviewer-r1 finding [H-1]. Files: `package.json`. Acceptance: `npm run postbuild` chain runs csp.mjs → verify:headers as adjacent steps.
- [ ] 1.6 Create `docs/hsts-stage.md` documenting the 4-stage HSTS rollout per the spec's "Staged HSTS Rollout" Requirement. Initial content: Stage 1 (`max-age=300`). Files: `docs/hsts-stage.md`. Acceptance: `verify:headers` reads this file to determine the expected `max-age` floor.

## 2. Phase 2 — Cloudflare Pages deploy workflow

- [ ] 2.1 Rename existing `.github/workflows/deploy.yml` → `.github/workflows/deploy-github-pages.yml` (preserve content verbatim; this is the SOAK-window GH Pages workflow). Files: workflow renamed. Acceptance: `git log --follow` shows the rename; existing GH Pages deploy continues to work.
- [ ] 2.2 Create new `.github/workflows/deploy.yml` for Cloudflare Pages. Use `cloudflare/wrangler-action@<SHA-pinned>` with `command: pages deploy ./.build/dist --project-name=artagon --branch=main`. Auth via `secrets.CLOUDFLARE_API_TOKEN` + `secrets.CLOUDFLARE_ACCOUNT_ID`. Trigger: push to main + workflow_dispatch. **Top of file MUST carry a header comment** referencing the rename (`# This file replaces the GH Pages deploy at deploy-github-pages.yml`) per multi-reviewer-r1 finding [L-4]. Files: `.github/workflows/deploy.yml`. Acceptance: workflow YAML lints clean; `wrangler-action` SHA is the latest pinned release.
- [ ] 2.3 Create `.github/workflows/deploy-cloudflare-pages-preview.yml` for PR preview deploys. Trigger: `pull_request` on opened/synchronize. Deploy to Cloudflare Pages; parse `wrangler pages deploy` output to extract BOTH the immutable deploy-id URL AND the branch-alias URL. Post BOTH URLs as a PR comment via `actions/github-script` with explicit labels ("SHA-pinned: ..." / "Branch latest: ..."). Per multi-reviewer-r1 finding [M-5]. Files: new workflow. Acceptance: workflow runs on PR open; comment lands with both URL labels.
- [ ] 2.4 Configure the preview environment to emit `X-Robots-Tag: noindex, nofollow` via Cloudflare Pages' deploy-environment configuration (NOT via `_headers` — `_headers` is path-based not host-based). Set via `wrangler pages project create` flags or post-creation API call. Files: documented in `docs/deploy.md`. Acceptance: `curl -I https://<deploy-id>.artagon.pages.dev` shows `X-Robots-Tag: noindex, nofollow`.

## 3. Phase 3 — Build the content-parity check (cutover-only tool)

- [ ] 3.1 Build `scripts/cutover/verify-content-parity.mjs`: accepts two URLs as args; walks `src/pages/**/*.astro` to enumerate routes; fetches each path against both URLs; normalizes response bodies (strip `<meta http-equiv="X-Pingback">`, dynamic timestamps, env-specific headers); computes SHA-256 hash per response; exits non-zero on any path whose hashes differ. Located under `scripts/cutover/` (not general `scripts/`) since it's a one-time tool. Per multi-reviewer-r1 finding [M-3]. Files: `scripts/cutover/verify-content-parity.mjs`. Acceptance: against two identical static deploys, exits 0; injecting a `<script>` into one of them flips to non-zero.
- [ ] 3.2 Add `node:test` tests: `tests/verify-content-parity.test.mjs`. Use a tiny HTTP fixture server (Node's `node:http`) to serve known content. Files: new test file. Acceptance: ≥ 6 tests covering happy path + drift detection + normalization edge cases.
- [ ] 3.3 Add npm script: `verify:content-parity` → `node scripts/cutover/verify-content-parity.mjs`. Files: `package.json`. Acceptance: invocable; `--help` flag prints usage.

## 4. Phase 4 — Document the cutover playbook (operational runbook)

- [ ] 4.1 Write `docs/deploy.md`: cutover playbook + rollback + post-deploy verification runbook. Per multi-reviewer-r1 finding [H-5] this is the runbook for the manual operational steps; tasks.md does NOT include the cutover steps themselves (they are not implementation work). Required sections:
  - § "Cutover Sequence" — Phase 0 prereqs, parallel-deploy guarantee, TTL drop, DNS swap, monitoring.
  - § "DNS Settle Window" — document 24-48h realistic settle (per multi-reviewer-r1 finding [M-4]); 1h is the MINIMUM pre-cutover wait, NOT global propagation.
  - § "Rollback" — labeled section; revert CNAME at registrar; recovery time ≤ 5 minutes.
  - § "Post-deploy Verification" — runbook for the production-fetch checks the spec marks as documented (security headers ship; cache rules apply; TLS 1.0/1.1 rejected; HTTP→HTTPS upgrade). Per multi-reviewer-r1 finding [H-4].
  - § "Origin Lockdown Settings" — Cloudflare project settings checklist (Always Use HTTPS, min TLS 1.2, Auto-Minify off).
  - § "HSTS Stage Rollout" — cross-references `docs/hsts-stage.md`; documents Stage 1 → 4 timeline.
  - § "Account Isolation" — documents that the security boundary is the dedicated CF account, not per-project token scoping.
    Files: `docs/deploy.md`. Acceptance: document is greppable for "Rollback", "DNS Settle", "Post-deploy", "HSTS Stage", "Account Isolation".
- [ ] 4.2 Update `AGENTS.md` deployment section: cite `docs/deploy.md` as the source of truth; remove obsolete GH Pages-only language; add a "Production deploy target: Cloudflare Pages" line. Files: `AGENTS.md`. Acceptance: any agent reading AGENTS.md gets the right deploy context.
- [ ] 4.3 Update `README.md` Deployment section if present. Files: `README.md`. Acceptance: README reflects current deploy target.

## 5. Phase 5 — Verification (CI-runnable; cutover lives in docs/deploy.md)

- [ ] 5.1 Run full local gate sweep: `npm run typecheck && npm run build && npm run postbuild && npm test && npm run lint:sg:ci`. Acceptance: all exit zero; postbuild chain includes `csp.mjs → verify:headers`.
- [ ] 5.2 Run `openspec validate --strict migrate-deploy-to-cloudflare-pages` once more. Acceptance: validation passes.
- [ ] 5.3 Push branch; confirm new `.github/workflows/deploy.yml` runs cleanly on `workflow_dispatch` (no actual production cutover until docs/deploy.md runbook is followed by a maintainer). Acceptance: PR shows both `deploy-github-pages.yml` (rename) AND `deploy.yml` (new Cloudflare Pages) workflows green on `workflow_dispatch`.
- [ ] 5.4 Open `.openspec.yaml` metadata gate: per multi-reviewer-r1 finding [L-5], add `implementation_pr_link` and `archive_deadline` fields to this change's `.openspec.yaml` so the proposal can't sit forever in `openspec/changes/` without matching code. Files: `openspec/changes/migrate-deploy-to-cloudflare-pages/.openspec.yaml`. Acceptance: `.openspec.yaml` declares an `implementation_pr_link` (TBD until Phase 1+ implementation lands) and `archive_deadline: <date 90 days from merge>`; openspec validate still passes. Per Copilot review on PR #45.

## 6. Phase 6 — Implementation acceptance (after cutover docs/deploy.md is followed)

- [ ] 6.1 Coordinate with `update-site-marketing-redesign`: USMR Phase 7.4 + 10.5 (the `/bridge` redirect entries) become actionable once `_redirects` is on a Cloudflare-Pages-supporting host. PR #45's PR description cross-references this. **`_redirects` jurisdiction:** USMR's `validate-indexation.mjs` (Phase 10.7) is the implementation of "same-origin destination" enforcement; this change's spec re-asserts the constraint at the deploy-platform contract layer. Per multi-reviewer-r1 finding M-3 (cross-PR jurisdiction note). Files: PR description. Acceptance: cross-ref documented.

## 7. Phase 7 — Capability factoring (deferred to follow-up)

- [ ] 7.1 **DEFERRED** to a follow-up OpenSpec change `factor-check-site-quality-capability`: per multi-reviewer-r1 finding [M-8], `check-site-quality` is becoming a junk drawer (PR #44 adds 2, PR #45 adds 2, modernize-vitest adds 3 ADDED Requirements). The follow-up will split into focused capabilities (`font-quality-gates`, `cross-host-deploy-verification`, `deploy-headers-gates`). For round-1 of THIS change, the 2 ADDED Requirements stay under `check-site-quality` to avoid scope explosion. Files: none in this change. Acceptance: the follow-up change directory exists as a stub at `openspec/changes/factor-check-site-quality-capability/` (created in Phase 7.2).
- [ ] 7.2 Create stub OpenSpec change `factor-check-site-quality-capability` with proposal.md + minimal `specs/<capability>/spec.md` so the directory passes `openspec validate --strict`. Files: NEW directory under `openspec/changes/factor-check-site-quality-capability/` containing proposal.md (refactor scope description), tasks.md (single placeholder task), and specs/check-site-quality/spec.md (one MODIFIED Requirement stub stating "Capability split is the subject of this change"). Acceptance: `openspec list` shows the stub; `openspec validate --strict factor-check-site-quality-capability` exits 0. (No "intentionally invalid" placeholder allowed — strict-valid is the deterministic outcome.) Per Copilot review on PR #45.

## 8. Phase 8 — Follow-up archive of GH Pages workflow (in proposal Sequencing, not tasks)

> Per multi-reviewer-r1 finding [H-5]: "Open follow-up OpenSpec change `archive-github-pages-deployment`" was a change-management task, not implementation. Moved to proposal.md "Sequencing" section. After 7-day soak documented in `docs/deploy.md`, the maintainer files the follow-up change manually.
