# Tasks — update-site-marketing-redesign

> Numbered for cross-reference from `proposal.md`, `design.md`, and capability deltas. Each task lists files touched and the acceptance signal. Tasks within a phase MAY parallelize; phases MUST land in order.

## Phase 0 — Pre-flight

- [x] 0.1 Confirm `refactor-styling-architecture` has merged; rebase on `main`. → ✅ archived at `openspec/changes/archive/2026-05-04-refactor-styling-architecture/` (merged via PR #39).
- [x] 0.2 Resolve any `public/assets/theme.css` token-name conflicts; if rename needed, update §6 token names. → ✅ audit found exactly **2 collisions**: `--accent` and `--bg` (live theme.css 95 tokens; upstream `new-design/extracted/src/styles/tokens.css` 19 tokens; `comm -12` overlap = 2). Phase 2 token-rename contract handles these via `--nd-accent`/`--nd-bg` prefixes per the conversion guidance doc; no §6 spec rename needed since both collisions stay namespaced.
- [x] 0.3 `openspec validate update-site-marketing-redesign --strict` — fix all schema issues. → ✅ green.
- [x] 0.4 Confirm CODEOWNERS includes the nine new capability dirs and the cross-cutting files (`public/assets/theme.css`, `src/layouts/BaseLayout.astro`, `astro.config.ts` (was `astro.config.mjs` pre-Astro-6 — actual file in this repo is `.ts`), `scripts/csp.mjs`, `scripts/sri.mjs`). → ⚠️ pre-audit state: CODEOWNERS had `astro.config.ts` + `csp.mjs` + `sri.mjs` but missing `BaseLayout.astro` + `theme.css`. **Fixed in this PR** (Phase-0 commit).
- [x] 0.5 Build `scripts/verify-prerequisites.mjs` (`npm run verify:prerequisites`): fails the build unless `openspec/changes/refactor-styling-architecture/` is archived OR its merge commit is an ancestor of `HEAD`. Wire into postbuild + PR CI. → ✅ shipped: 4-state script (a/b/c/d) covering archived / archived+ancestor / orphan-tree / in-flight. 5 node:test cases. Wired into `package.json` postbuild + `.github/workflows/design-md-lint.yml`.
- [x] 0.6 Tag the pre-redesign baseline of `BaseLayout.astro`, `theme.css`, and `csp.mjs` as `pre-redesign` once `refactor-styling-architecture` archives — used by the multi-phase rollback path documented in `proposal.md`. MUST `git push origin pre-redesign` so fresh CI clones see the tag (local-only tags break CI rollback). Add a CI pre-merge check `git rev-parse --verify pre-redesign 2>/dev/null` to confirm the tag exists on origin before any Phase 2+ task can run. → ✅ annotated tag created at `0e05105` (head of main post PR #40), pushed to origin (`refs/tags/pre-redesign` → SHA `462482e`). The CI pre-merge check is a follow-up task for Phase 2 (no Phase 2 tasks land in this PR).

- [x] 0.7 Adopt the conversion guidance prompt at `docs/guides/new-design-conversion.md` (v3.2, post-3-round adversarial review by Claude/Codex/Gemini). → ✅ doc shipped at `docs/guides/new-design-conversion.md` (391 lines). Note: section v3.2's React-island guidance was further updated in commit `290f111` (during PR #39) to permit React islands, since `@astrojs/react@5.0.4` was installed for the Tweaks panel — the React-NOT-installed clause below is **superseded by current state** but kept as historical context. The current React-island posture is documented in the conversion guide itself. Mandates from v3.2:
  - **Skill mandate**: load `tdd`, `typed-service-contracts`, `agent-dx-cli-scale`, and `design-md` skills from `.agents/skills/` before any conversion edit.
  - **CSP runtime blocker**: new-design HTML mocks load `unpkg.com` React/Babel CDN (e.g. `new-design/extracted/src/pages/index.html` lines 429-431) and Google Fonts CDN. Live CSP at `scripts/csp.mjs:23` is `script-src 'self'` + hashes only. Any conversion that copies mock script tags verbatim breaks runtime even if build passes. Self-host fonts under `public/assets/fonts/` per Phase 2.3 BEFORE running first page conversion.
  - **React-island guidance**: `@astrojs/react` is NOT YET installed. React islands are PERMITTED — and expected for forward-looking interactive surfaces (`/play` playground, `/console` admin, `/search` typeahead, hero trust-chain animation, Bridge SVG carousel). For each island, document the cost (bundle bytes, CSP/SRI hash regen, dep additions: `@astrojs/react` + `react` + `react-dom` + `@types/react` + `@types/react-dom`) in the conversion commit. Prefer cheaper alternatives (CSS-only `:checked`/`:has()`/`<details>`/`popover` API, vanilla `<script>` blocks, or web components like `src/scripts/tweaks.ts`) when they suffice. Marketing routes (`/`, `/platform`, `/vision`, `/roadmap`, etc.) STAY static — only interactive surfaces hydrate. Self-host all WOFF2 fonts under `public/assets/fonts/` to keep CSP `font-src 'self'` (no Google Fonts CDN).
  - **Full absorption**: USMR replaces the live marketing surface with the new-design content. Every page in `src/pages/` corresponds 1:1 with a conversion target; once USMR archives, `new-design/extracted/` becomes a historical reference and may be removed in a follow-up `cleanup-new-design-extracted` change. There is no dual-tree state — either content lives in `src/` or it does not exist on the live site.
  - **Token rename contract**: before merging `new-design/extracted/src/styles/tokens.css`, sed-rename ALL its tokens to `--nd-*` prefix (`--bg` → `--nd-bg`, `--accent` → `--nd-accent`, etc.). Append the renamed block to `public/assets/theme.css` under `/* ===== NEW-DESIGN TOKENS (OKLCH) ===== */`. Per-converted-page, alias old tokens via `:root[data-theme="midnight"] { --bg: var(--nd-bg); }` ONLY after visual verification. Never merge new-design tokens directly into a plain `:root {}` block — it would shadow the three live theme packs (`midnight`/`twilight`/`slate`).
  - **Route map authority**: live routes are NESTED at `src/pages/<route>/index.astro` (14 of 16); only `index.astro` and `404.astro` are flat. Conversion replaces page CONTENT but MUST preserve the route paths. New-design's HTML mocks have non-overlapping routes (`bridge`, `blog`, `brand-icons`, `use-cases`, `standards`, `post-*.html`) — do NOT create new routes outside the 16 live routes + 4 USMR-added routes (`/use-cases`, `/standards`, `/writing/[slug]`, `/writing/feed.xml`). Discard `bridge.html` (merged into `/platform#bridge`), `blog.html` (superseded by `/writing/[slug]`), `brand-icons.html` (defer to `add-brand-assets-and-writing-pipeline`).
  - **BaseLayout preserve contract**: lines 16-37 of `src/layouts/BaseLayout.astro` are the pre-paint theme-persistence script — DO NOT touch (theme flash if removed). All 4 slots (`head`, `header`, default body, `footer`) must remain.
  - **Hook-location reference**: the v3.2 prompt has a per-component table at `new-design/extracted/src/components/*.jsx` of every `useState`/`useEffect`/`setInterval` line and the recommended Astro alternative. Use it verbatim during conversion.

## Phase 1 — Capability scaffolding

- [x] 1.1 Spec deltas live under `openspec/changes/update-site-marketing-redesign/specs/{site-content,site-navigation,style-system,site-standards-registry,site-bridge-story,site-mobile-layout,site-structured-data,site-indexation,site-branding}/spec.md`. → ✅ all 9 delta directories present and `openspec validate --strict` green.
- [x] 1.2 Update `openspec/project.md` with new routes, new capabilities, merge-order note with `refactor-styling-architecture`. → ✅ Capabilities + Merge order sections added; routes list extended with `/use-cases`, `/standards`, `/writing/[slug]`, `/writing/feed.xml`; stale `dist/` paths corrected to `.build/dist/`; tech stack updated (MDX, Content Collections + Zod, Playwright, ast-grep, `@astrojs/react`).
- [x] 1.3 Update `openspec/config.yaml` context block with new capability names. → ✅ Stale "Active in-flight change: refactor-styling-architecture" block replaced with current Live capabilities + In-flight changes + Merge order. Layout section notes USMR additions (writing collection, named slots, theme packs).
- [x] 1.4 `openspec validate update-site-marketing-redesign --strict` passes — gate before any code lands. → ✅ green at this commit.

## Phase 2 — Design-system foundations (style-system)

- [~] 2.1 Add raw OKLCH palette + semantic aliases + token categories (motion, elevation, focus-ring, z-index, spacing, radius) to `public/assets/theme.css` behind cascade layers `@layer reset, tokens, utilities, components, overrides`. (NEW tokens are layered; ~150 legacy `:root` declarations remain unlayered to preserve cascade priority per "Existing Token Preservation". Spec language `"All token definitions MUST live in tokens layer"` will be reconciled by stub change `openspec/changes/migrate-legacy-tokens-to-layer/` (filed; authoring deferred until USMR archives).)
- [~] 2.2 Define `twilight` (modified — extends existing requirement) and `midnight` (new) under `[data-theme="..."]`. (Theme blocks defined; theme-aware fallbacks via `--brand-teal-rgb` per-theme. Axe-core verification deferred — `@axe-core/playwright` not yet installed; lands with the broader Playwright/axe install in Phase 6 quality gates.)
- [ ] 2.3 Self-host WOFF2 fonts under `public/assets/fonts/` (Inter Tight, Space Grotesk, Fraunces, Instrument Serif, JetBrains Mono); subset with `unicode-range`; ship `LICENSE.txt`. Non-display families (Fraunces, Instrument Serif) MUST NOT load on routes that don't use them. → **superseded** by `self-host-woff2-fonts` Phase 1 (tasks 1.1–1.6) — that change owns binary download, subsetting, LICENSE.txt, CHECKSUMS, and the `public/assets/fonts/UPSTREAM` provenance file. Closes here when SHWF Phase 1 ships.
- [ ] 2.3a Emit exactly one `<link rel="preload" as="font" type="font/woff2" crossorigin>` per route, pinned to the LCP-critical face (Space Grotesk for marketing routes per DESIGN.md). Playwright asserts `getComputedStyle(document.querySelector('h1')).fontFamily` resolves to the preloaded family. → **superseded** by `self-host-woff2-fonts` Phase 4 (tasks 4.1–4.3) — `BaseLayout.astro` accepts `preloadFonts?: string[]` (default `["space-grotesk/500-normal"]`); per-route preload count is 1–2 (1 for marketing, 2 for `/writing/*` long-form); Playwright test lives at `tests/font-loading.spec.ts`. Closes here when SHWF Phase 4 ships.
- [ ] 2.3b Build `scripts/measure-font-payload.mjs` (`npm run measure:font-payload`): fails the build if total WOFF2 per route exceeds 180 KB OR per-family exceeds 60 KB. Wire into postbuild. → **superseded** by `self-host-woff2-fonts` Phase 5 (tasks 5.1–5.3). Closes here when SHWF Phase 5 ships.
- [ ] 2.4 `@font-face` blocks emit `size-adjust`, `ascent-override`, `descent-override`, `line-gap-override` derived from fallback-font metrics. → **superseded** by `self-host-woff2-fonts` Phase 3 (tasks 3.1–3.3) — typography section added to `theme.css` with full override quad; values populated by `derive:font-metrics --write`. Closes here when SHWF Phase 3 ships.
- [ ] 2.5 Build `scripts/derive-font-metrics.mjs` and `scripts/verify-font-metrics.mjs`; wire into `npm run verify`. → **superseded** by `self-host-woff2-fonts` Phase 2 (tasks 2.1–2.4) — both scripts plus tests + npm scripts. Closes here when SHWF Phase 2 ships.
- [x] 2.6 Emit fluid-type `clamp()` scale per `design.md` §6 table; floors at 360 px, ceilings at 1440 px.
- [x] 2.7 Update CSP `font-src` to `'self'` only — remove any third-party host. Update `scripts/csp.mjs` accordingly.
- [~] 2.8 Build `scripts/lint-tokens.mjs` (forbid hex/rgb/hsl/oklch literals, raw px/em/rem, spacing magic numbers in `.astro`/`.css`/`.mdx` outside `public/assets/theme.css`). Where the rule fits ast-grep, add a `rules/security/no-raw-color-literal.yaml` instead. (Color subset implemented + ast-grep companion rule shipped. Spacing/sizing enforcement (raw px/em/rem) deferred to Phase 12 quality gates — requires designing the allowlist for legitimate uses (line-height: 1.5, calc() math, clamp() formulas). Tracked in `openspec/changes/migrate-legacy-tokens-to-layer/` Phase 0.2.)
- [~] 2.9 Wire `lint:tokens` and `verify:font-metrics` into `npm run postbuild` and CI. (`lint:tokens` wired; `verify:font-metrics` wiring **superseded** by `self-host-woff2-fonts` Phase 6 task 6.1, which owns the full postbuild chain `verify:prerequisites && verify:design-prerequisites && lint:tokens && verify-font-self-hosting && verify:font-metrics && measure:font-payload && verify:font-subset-coverage && sri && csp && lint:design && lint:design-md-uniqueness`. Closes here when SHWF Phase 6 ships.)

## Phase 3 — Navigation + site chrome (site-navigation)

- [x] 3.0 Define the BaseLayout slot ABI: add `<slot name="json-ld">`, `<slot name="indexation">`, `<slot name="branding">` to `BaseLayout.astro`. After this lands, Phases 9, 10, 11 use `<JsonLd/>`, `<Indexation/>`, `<Branding/>` wrapper components and DO NOT directly edit `BaseLayout.astro`. → ✅ three named slots added to `<head>` after `<slot name="head">`; preceding pre-paint theme-persistence script (lines 17-38) untouched per Phase-0 contract; 4 existing slots (`head`, `header`, default, `footer`) preserved.
- [~] 3.1 `Nav.astro`: wordmark + 4 nav items (Platform · Use cases · Standards · Writing) + GitHub icon; `aria-current="page"`; sentence case; sticky 64 px backdrop-blurred. → ✅ component + chrome shipped; grid-based `auto 1fr auto` layout; `color-mix(in srgb, var(--bg) 72%, transparent)` backdrop-blur; accent underline on active/hover; GitHub Octicon icon-button (34×34, bordered square); all CSS tokens via `var(--*)` (no hex fallbacks); focus-visible rings; `@media (max-width: 640px)` collapses links (hamburger deferred to 3.3/3.4). **Route-list deferred:** spec calls for 4 items (Platform · Use cases · Standards · Writing) but `/use-cases`, `/standards`, `/writing` ship in USMR Phase 4-5. Live-only nav (Platform · Vision · Roadmap · Docs) shipped in the interim to keep lychee green; restore the spec'd 4 items when Phase 4-5 routes exist.
- [~] 3.2 `SkipLink.astro` + `<main id="main-content" tabindex="-1">` wrapper in `BaseLayout.astro`. Skip link is first focusable element. Activation moves focus into `<main>` (verified by `scripts/lint-skip-link.mjs` AND a Playwright scenario asserting `document.activeElement === main` after click). → ✅ `SkipLink.astro` shipped at `src/components/SkipLink.astro` with optional `href` / `label` props; `BaseLayout.astro` `<main>` now `id="main-content" tabindex="-1"`; lint side of acceptance covered by 3.9. Playwright `document.activeElement === main` assertion deferred — lands with the Phase 6 quality-gates Playwright bundle (axe + visual regression).
- [ ] 3.3 CSS-only hamburger baseline (checkbox pattern) for `< 720 px`.
- [ ] 3.4 `NavToggle.astro` progressively-enhanced Astro island (button + aria + focus trap + Esc close + reduced-motion respect).
- [x] 3.5 `Footer.astro`: 4-column layout (Platform · Developers · Company · Legal) + tagline + theme toggle + auto-year copyright + version string `v{semver} — build {7-char git sha}`. → ✅ `src/components/Footer.astro` replaced; brand column (wordmark + `tagline.positioning` + ThemeToggle); 4 link columns (Platform: /platform, /vision, /roadmap · Developers: /docs, /developers, /play, /status · Company: /faq, /get-started, /how · Legal: /privacy, /security, /security.txt) — restricted to live routes; `/use-cases`, `/standards`, `/writing` deferred to Phase 4-5; meta strip (`© {YEAR} Artagon, Inc. — Philadelphia, PA` · "Built on Astro · Open-source core · Apache 2.0" · `v{version} — build {7-char sha}`); SHA prefers `process.env.GITHUB_SHA` (CI) → `git rev-parse --short HEAD` (local) → `'unknown'` fallback; spacing via `var(--space-*)` tokens; responsive 2-col at ≤860px.
- [x] 3.6 `ThemeToggle.astro` `<button>` Astro island with `aria-pressed` reflecting active theme + inline pre-paint script (CSP-hashed) reading `localStorage.theme`, validating against allow-list `['twilight', 'midnight']`, falling back to `twilight` on any other value, and applying `<html data-theme>` via `setAttribute` only (no `innerHTML`/`outerHTML`/string-concatenated attribute construction). Update `scripts/csp.mjs` to include the new inline-script SHA-256 AND to fail the build if any inline `<script>` SHA-256 in `dist/` is not present in the emitted `script-src` directive (orphan-hash detection); forbid `'unsafe-inline'`. → ✅ `ThemeToggle.astro` rewritten as `<button data-theme-toggle>` with sun/moon SVG icons (CSS-toggled by `aria-pressed`); `is:inline` pre-paint aria-sync script + bundled click handler; all mutations via `setAttribute` only; allow-list `['midnight','twilight']` enforced at every entry point. `BaseLayout.astro` pre-paint validates `localStorage.theme` against the same allow-list; out-of-list values collapse to `twilight` (project default). Static `<html data-theme="twilight">` flipped from `midnight` to match the new default. `__setTheme` mirror validates and syncs `aria-pressed` on every `[data-theme-toggle]` button. `scripts/csp.mjs` orphan-hash detection: after building policy, verifies (a) `'unsafe-inline'` not in `script-src`, (b) every inline script SHA-256 is present in `script-src`; post-write self-audit re-reads each `.html` file and re-extracts hashes, throwing if any orphan slipped through cheerio's serialize cycle. `csp.mjs` exports `buildPolicy`, `extractScriptSrcHashes`, `sha` for unit testing (guarded with `process.argv[1] === fileURLToPath(import.meta.url)`); `tests/csp.test.mjs` (8 cases) covers helper round-trip, orphan detection, and `'unsafe-inline'` exclusion. Dev-only `ThemePreviewPanel.astro` reduced to `['twilight','midnight']` to match the production allow-list.
- [ ] 3.7 `SiteHead.astro` (or extend `BaseLayout.astro`): title, description, canonical (path-only check at lint), viewport, OG/Twitter, robots prop, favicons.
- [ ] 3.8 `scripts/lint-meta.mjs`: description 80–160 chars, canonical present (path-only against `https://artagon.com`, no `?` query strings), exactly one `<h1>` per page, and writing posts have a non-skipping h1→h2→h3 ladder.
- [x] 3.9 `scripts/lint-skip-link.mjs` (or Playwright equivalent): every built page in `dist/` has skip link as first tabbable element. → ✅ shipped at `scripts/lint-skip-link.mjs`; reads dist root from `build.config.json` (`.build/dist/`); walks all `*.html`; asserts first focusable element in `<body>` is a `class="skip-link"` anchor AND its `href="#<id>"` target id exists on the page; npm script `lint:skip-link`; wired into `postbuild` chain after `csp` and before `lint:design`. Verified locally against `.build/dist/` (16 pages, all pass).
- [x] 3.10 `scripts/lint-taglines.mjs`: enforce single source for `tagline.short` and `tagline.triad` from `src/content/taglines.json`. → ✅ `src/content/taglines.json` created with `tagline.{positioning,full,short,abbr,inline,triad}`; `scripts/lint-taglines.mjs` walks `src/**/*.{astro,ts,tsx,mjs}` (excludes `.mdx` prose); flags any file containing a guarded string verbatim; exit 0/1/2 contract; npm script `lint:taglines`; wired into `postbuild` after `lint:skip-link`; `tests/lint-taglines.test.mjs` (7 cases); `Tagline.astro`, `SeoTags.astro`, `src/pages/index.astro` updated to import from taglines.json — all 47 source files now pass.

## Phase 4 — Content collections (site-content)

- [x] 4.1 Extend Zod schemas in `src/content.config.ts` for `pages`, `pages/writing`, and `authors` collections (eyebrow, headline, lede, CTAs[], heroFont?, accent?, tags[]). → ✅ `src/content.config.ts` updated (Astro 6 path — the Phase 4.1 commit mistakenly created `src/content/config.ts` which Astro silently ignored; corrected in Phase 4.6 commit `cd005b4`). `pages` schema requires `title`, `description`, `eyebrow`, `headline`, `lede`, `ctas[]` per spec site-content §"Type-Safe Content Schemas"; accepts optional `heroFont` (`space-grotesk` / `fraunces` / `inter-tight`), `accent`, `tags[]`. `writing` sub-collection extends `pages` with required `published` (z.coerce.date) and accepts optional `updated`, `cover`, `repo` (z.url), `author`, `draft` (default false). `authors` collection requires `name`, `slug`; accepts optional `bio`, `avatar`, `links[]` (each `{label,href}` with z.url). `cta` shape: `{label, href, rel?, variant?}` where variant ∈ `primary|secondary|ghost`. Existing `vision.mdx` migrated to satisfy the new contract (added `eyebrow`, `headline`, `lede`, `ctas[]`; description rewritten to 155 chars (within the lint-meta 80–160 band)). Build passes Zod validation; `pages/writing/` and `authors/` collections inert until populated in Phase 4.2 / 4.5.
- [~] 4.2 Author `src/content/pages/{home,platform,use-cases,standards,roadmap,writing}.mdx` with frontmatter per `design.md` §4. → ✅ frontmatter shipped: 5 new MDX files (`home.mdx`, `platform.mdx`, `use-cases.mdx`, `standards.mdx`, `roadmap.mdx`) ported from `new-design/extracted/src/pages/*.html` mocks. Each file's `eyebrow`, `headline`, `lede`, `ctas[]` mirror the corresponding mock's `<div class="eyebrow">` / `<h2>` / `<p class="lead">` / `<a class="btn primary">` selectors. Description rewritten to fit lint-meta's 80–160 char band per page. **Bodies are stub-only** (1-2 lines each describing what Phase 5.x will populate) — the actual marketing copy (pillar grids, scenario cards, standards registry, roadmap phases) ships in Phase 5.1–5.5 alongside the route templates that consume them. The `writing.mdx` index page is **not** authored here; `/writing` is a route renderer (Phase 4.6), not a content entry. Vision page already migrated in 4.1. All 6 page entries Zod-validate against the schema from 4.1.
- [x] 4.3 Add `bridge: { sentence, variants[] }` frontmatter to `platform.mdx`. → ✅ `src/content.config.ts` extended with optional `bridge` field on `pageBase` (`{sentence: z.string().min(1), variants: z.array(z.string()).default([])}`). `platform.mdx` populated: canonical sentence ported from `new-design/extracted/src/pages/bridge.html` ("Verifiable Credentials don't require a rewrite. Artagon's OID4VP→OIDC bridge…"); 3 approved variants for use on home/use-cases per the lint-bridge allow-list contract. Other pages leave `bridge` unset. lint-bridge.mjs (Phase 5.x) will enforce single-source on `/platform` and forbid verbatim repetition elsewhere.
- [x] 4.4 Author `src/content/taglines.json` with `tagline.short` (≤ 60 chars) and `tagline.triad` (3 clauses). → ✅ shipped by Copilot in commit `306913c` alongside `lint-taglines.mjs`. Spec-checked: `short` is 28 chars ("Verified, Private, Attested."); `triad` has exactly 3 clauses ("Every claim, proven." / "Every token, bound." / "Every decision, traceable."). Also includes `positioning`, `full`, `abbr`, `inline` variants for legitimate consumers (footer tagline, hero, meta description). All consumed via `import taglines from '../content/taglines.json'`; lint-taglines (3.10) enforces single-source.
- [x] 4.5 Sample `src/content/pages/writing/welcome.mdx` (non-draft) to exercise the writing detail route. → ✅ shipped at `src/content/writing/welcome.mdx` (relocated from `pages/writing/` — see 4.6 note for rationale). Content adapted from `new-design/extracted/src/pages/post-trusted-identity.html` (h1 + lede + 3-section body — Why now / What Artagon builds / What's next). Frontmatter satisfies the writing schema: `published: 2026-05-06`, `draft: false`, `tags: [announcement, identity, vc, oidc]`.
- [x] 4.6 Wire `/writing`, `/writing/[slug]`, `/writing/feed.xml` (`@astrojs/rss`). → ✅ three routes shipped: `src/pages/writing/index.astro` (post list, sorted desc by `published`, drafts filtered, empty state hides the list), `src/pages/writing/[slug].astro` (post detail with `getStaticPaths`, hero from frontmatter, body via `<Content/>`, optional `updated` marker), `src/pages/writing/feed.xml.ts` (RSS via `@astrojs/rss@4.0.18` with title/description/link/pubDate per item, `<language>en-us</language>` customData, drafts filtered). **Important deviation from spec literal**: the spec calls for posts under `src/content/pages/writing/`, but Astro 6's content config file is `src/content.config.ts` (not `src/content/config.ts` as in earlier Astro versions) and the content-layer's `glob` loader cannot scope a sub-collection to a subdirectory of another collection's base when both bases overlap. The pragmatic resolution: collection root moved to `src/content/writing/`; the spec's intent (separating writing from marketing pages, single-source frontmatter contract) is preserved verbatim. Build emits 18 routes; RSS contains 1 `<item>` (the welcome post); index lists 1 post linked to `/writing/welcome/`.
- [ ] 4.7 Update `openspec/specs/site-content/spec.md` Purpose line during archive (replace TBD).

## Phase 5 — Marketing routes (site-content + style-system)

- [~] 5.1 `/` hero + on-ramp card + pillar grid + standards row + latest-writing strip + latest-roadmap-update card. → in flight; sub-tasks below track slice-by-slice landing.

  Content slices (frontmatter + typed registries — visual transition tracked separately under "Visual transition" below):
  - [x] 5.1a Hero reads from `home.mdx` frontmatter (eyebrow · headline · lede · CTAs). Shipped 18fbb6f / 8c0c565.
  - [x] 5.1b On-ramp card from `home.mdx` `onRamp` + typed `ORG` (`src/data/organization.ts`). Shipped 99f55ed / 0b7eef0.
  - [x] 5.1c Pillar grid from typed `PILLARS` (`src/data/pillars.ts`). Shipped bcae823 / 96f3583.
  - [x] 5.1d Trust-chain — three sub-stages:
    - [x] 5.1d-data Typed `STAGES` + `SCENARIOS` registry + vitest invariants (`src/data/trust-chain.ts`, `tests/trust-chain-data.test.mts`, `vitest.config.ts`). Shipped d0b7721 / 92ce808.
    - [x] 5.1d-static Static SSR rendering of `SCENARIOS[0]` in hero-art column. Shipped 65cad93 / 4c868dc (later superseded by the island below; component file deleted in dfb47c5).
    - [x] 5.1d-react Interactive React island (`TrustChainIsland.tsx`) with scenario picker + hover-to-claim. Shipped e8d0929 / 1381edb. Token + a11y polish in 47f9898 / d489ded; `--bad / --ok / --warn` aliases promoted from `--nd-*`.
    - **Note on 4c868dc / 1381edb commit messages:** both message bodies contain the line "lint:tokens accepts oklch() without flagging it as a raw color literal" — this claim is **factually wrong**. `scripts/lint-tokens.mjs` line 56 forbids oklch outside the `public/assets/theme.css` + `DESIGN.md` allowlist; the gate only "passed" during those commits because `git ls-files` excluded the still-untracked `TrustChainIsland.css`. Once committed (1381edb) the gate would have hard-failed any subsequent `npm run build`, and was caught + corrected in 47f9898 / d489ded. History is immutable; this note is the canonical disclaimer.
  - [x] 5.1d-idle Trust-chain auto-progression. SSR keeps the fully-resolved state (step = STAGES.length) so pre-hydration HTML reads correctly for crawlers + Playwright; on hydration a `useEffect` resets `step` to 0 and walks it forward via `setTimeout` chain (400 ms kickoff, 1100 ms first-stage, 900 ms per-stage; halts on first `fail`). `is-evaluating` stages get `chain-pulse` on the number circle + `chain-spinner` next to "checking…" status. `is-pending` (not yet reached) stages mute to `--fg-3`. Skipped under `prefers-reduced-motion: reduce` AND under `navigator.webdriver` (Playwright deterministic E2E) — the chain renders fully resolved with no timer in those contexts. First-mount only — subsequent scenario changes settle immediately. DESIGN.md §6.5 "Animation primitives" + "Auto-progression timing" updated in lock-step.
  - [ ] 5.1e Standards row → blocked by Phase 6 (STANDARDS registry).
  - [x] 5.1f Latest-writing strip — top 3 non-draft posts from the writing collection. Shipped 55db7e4 / f42f54c.
  - [ ] 5.1g Latest-roadmap-update card → blocked by Phase 5.7 (roadmap redesign defines its shape).

  Visual transition (new-design parity — bridges the gap between content scaffolding and the `new-design/extracted/` mock):
  - [ ] 5.1h Token alias migration — public aliases over the `--nd-*` staging set, mirroring the `--bad / --ok / --warn` pattern shipped in 47f9898. Old aliases retained as deprecation shims for one release, dropped in a follow-up.
    - [ ] 5.1h.1 Add `--fg`, `--fg-1`, `--fg-2`, `--fg-3`, `--accent`, `--accent-dim`, `--accent-ink`, `--bg-1`, `--bg-2`, `--line`, `--line-soft` aliases to `public/assets/theme.css` under the existing token band (lines 42-48 sibling).
    - [ ] 5.1h.2 Migrate `src/components/TrustChainIsland.css`: `var(--text)` → `var(--fg)`, `var(--muted)` → `var(--fg-2)`, `var(--border)` → `var(--line)`, `var(--bg-alt)` → `var(--bg-1)`. The `var(--ok) / var(--bad)` references stay (already public aliases).
    - [ ] 5.1h.3 Migrate `src/pages/index.astro` scoped `<style>` block to the same alias swap.
    - [ ] 5.1h.4 Migrate `src/components/Footer.astro` scoped styles. (Header.astro is rebuilt in 5.1l, no migration needed.)
    - [ ] 5.1h.5 Build green; postbuild lints pass; 45/45 visual snapshots stable on chromium-darwin (the swap is value-equivalent — `--bg-alt = --nd-bg-1 = --bg-1`, etc., so pixels don't shift).
  - [ ] 5.1i Glow component CSS port — bring `.glow-*` from `new-design/extracted/src/styles/global.css` lines 115-210 into `public/assets/theme.css`. Pure CSS, no JS.
    - [ ] 5.1i.1 Port `.glow-tag` + `.glow-tag::before` (conic-gradient ring) + `.glow-tag::after` (radial-gradient halo). Honors `--g-accent` CSS custom property.
    - [ ] 5.1i.2 Port `.glow-dot` (radial gradient + box-shadow) + `.glow-dot::before` + `.glow-dot::after` (animated rings).
    - [ ] 5.1i.3 Port `.glow-text` (linear-gradient + background-clip:text shimmer) and `.glow-amp` (serif italic ampersand with text-shadow).
    - [ ] 5.1i.4 Port keyframes `glow-breathe`, `glow-halo-drift`, `glow-ring-ping`, `glow-text-shimmer`. Add `@media (prefers-reduced-motion: reduce) { animation: none }` for all four.
    - [ ] 5.1i.5 Build + lint:tokens green (no raw color literals; all glow CSS uses `--g-accent` or token aliases).
  - [ ] 5.1j Hero structure rebuild — match the new-design mock layout (eyebrow pill + stacked headline). `eyebrow / headline / lede / ctas[]` continue to flow from `home.mdx`.
    - [ ] 5.1j.1 Replace the inline `<p class="hero__eyebrow">{eyebrow}</p>` paragraph with the `.glow-tag` pill: `<span class="glow-tag"><span class="glow-dot"></span><span class="glow-text">Trusted identity for machines<em class="glow-amp">&amp;</em>humans</span></span>`.
    - [ ] 5.1j.2 Stack the headline. Frontmatter `headline: "Verified. Private. Attested."` → `index.astro` splits on `. ` and renders each clause on its own `<br/>`-separated line (no MDX change). Class `display` to opt into the new-design display typography.
    - [ ] 5.1j.3 Remove `<Tagline />` and the 4-pill `.badges` cluster from the hero. The standards row moves to 5.1e below the CTAs when 6.x ships; the tagline folds into the meta-strip (5.1k).
    - [ ] 5.1j.4 Regenerate `home-hero-*-{theme}-{bp}.png` baselines (Darwin local + workflow_dispatch on Linux).
  - [ ] 5.1k Hero meta-strip + affiliations ticker.
    - [ ] 5.1k.1 Add the 3-slot meta-strip ABOVE the hero `<section>` in `index.astro`: left `v0.9 · Design Partner Preview` (mono uppercase) · center OIDC / GNAP / OID4VC / Zanzibar / Cedar (dotted-underline external links to canonical specs) · right `US — Philadelphia, PA` (mono uppercase). All 12 px on `var(--fg-2)`.
    - [ ] 5.1k.2 Create `src/data/affiliations.ts` exporting typed `AFFILIATIONS` array — 6 entries (IETF GNAP / OpenID OID4VC / W3C DIDs / W3C VCs / NIST 800-63 / eIDAS 2), each `{label, href, title}`. Add `tests/affiliations-data.test.mts` (vitest) for shape invariants.
    - [ ] 5.1k.3 Render the "Implements & contributes to" affiliations ticker below hero CTAs in `index.astro`. Each item is a dotted-underline external link with `target="_blank" rel="noopener noreferrer"` and `title` carrying the spec citation.
    - [ ] 5.1k.4 Regenerate hero baselines (meta-strip changes pixel layout above the fold).
  - [ ] 5.1l Header redesign. Updates `src/components/Header.astro` to match new-design `global.css` lines 250-261 nav contract.
    - [ ] 5.1l.1 Update route list to `Platform / Bridge / Use cases / Standards / Roadmap / Blog / GitHub`. Drop `Vision / FAQ / Developers / Docs` from the visible nav (routes still resolve when typed). `Blog` aliases `/writing` for now (a `/blog` redirect ships in a follow-up).
    - [ ] 5.1l.2 Add right-slot CTAs: `Playground` ghost button → `/play`; `Request access` primary purple button → `/get-started`.
    - [ ] 5.1l.3 Sticky positioning with `position: sticky; top: 0; z-index: 30; backdrop-filter: blur(14px); background: color-mix(in oklab, var(--bg) 72%, transparent); border-bottom: 1px solid var(--line-soft);`.
    - [ ] 5.1l.4 Update `scripts/lint-skip-link.mjs` allowlist if the skip-link target id shifts; verify with `npm run lint:skip-link`.
    - [ ] 5.1l.5 Regenerate every visual snapshot (the header is above-the-fold on every page; all hero baselines and the vision baselines need refresh).
  - [ ] 5.1m Body backdrop grid. Adds the 88 × 88 px grid pattern from new-design `global.css` lines 79-91 to every page.
    - [ ] 5.1m.1 Add `body::before` rule in `src/layouts/BaseLayout.astro` global `<style is:global>` block: two stacked `linear-gradient` lines (1 px on `var(--line-soft)`) + `radial-gradient` ellipse mask centered at 50% 20%, opacity 0.35, `pointer-events: none`, `z-index: 0`.
    - [ ] 5.1m.2 Add `body.no-grid::before { display: none }` for routes that ship their own visual (e.g. `/play`). Default ON; opt-out per page.
    - [ ] 5.1m.3 Hoist `#root { z-index: 2 }` so content renders above the backdrop.
    - [ ] 5.1m.4 Regenerate all baselines (backdrop is visible everywhere).
  - [ ] 5.1n Legacy section prune. After this lands, `/` matches the 5.1 spec list verbatim.
    - [ ] 5.1n.1 Delete the KPI row (Decision latency / Token issuance / VC verification / Availability) from `src/pages/index.astro`. KPI numbers will move to `src/data/kpis.ts` and a `/platform` pillar-band consumer in Phase 5.2 (separate commit).
    - [ ] 5.1n.2 Delete the `#how` "How it works" section + flow card from `src/pages/index.astro`. The numbered flow becomes a `/platform` band in 5.2.
    - [ ] 5.1n.3 Delete `<MissionSection />` import + usage. Verify the content already lives in `/vision` (Phase 4.1). If gaps, fold the missing copy into `vision.mdx` first.
    - [ ] 5.1n.4 Delete the `#developers` section + curl quickstart card. Quickstart moves to `/developers` (route already exists).
    - [ ] 5.1n.5 Delete the `#faq` section + 4 details/summaries. `/faq` route exists and remains canonical.
    - [ ] 5.1n.6 Drop the now-unused imports (`MissionSection`). Trim scoped CSS that targeted the removed sections.
    - [ ] 5.1n.7 Build green; visual snapshots regenerated; verify `tests/home.spec.ts` smoke test still passes (it asserts hero / pillars / writing-strip / on-ramp visibility — all retained).
  - [ ] 5.1o Visual chrome polish — close the gap between the shipped 5.1h-5.1n structure and the new-design mock's editorial finish. Surfaced when the user compared the post-prune render against `new-design/extracted/src/pages/index.html` and called out 5 outstanding deltas.
    - [ ] 5.1o.1 Default accent flips to violet. Add `data-accent="violet"` on `<html>` in `BaseLayout.astro`. Ports the variant rules from new-design `global.css` lines 61-63 to `public/assets/theme.css`. The mock screenshot uses the violet variant; matching it is the design intent.
    - [ ] 5.1o.2 Default hero font flips to serif. Add `data-hero-font="fraunces"` on `<html>` in `BaseLayout.astro`. Ports `[data-hero-font="fraunces"]` (and grotesk / dmserif / mono variants) from new-design `global.css` lines 38-41 to `public/assets/theme.css`. Adds the four `--f-sans / --f-mono / --f-serif / --f-display` tokens to the `:root` block. Fraunces falls back to Georgia until self-host-woff2-fonts ships the WOFF2.
    - [ ] 5.1o.3 Header gains the "Artagon" wordmark. `Header.astro` renders the `<img>` icon (now `aria-hidden`) followed by `<span class="site-nav__wordmark">Artagon</span>` consuming `--f-display` for typographic continuity with the headline.
    - [ ] 5.1o.4 `.btn.primary` in the nav renders solid-filled per new-design `global.css :245` rather than the legacy theme.css ghost-translucent style. Header.astro scoped CSS overrides `background: var(--accent)`, `color: var(--accent-ink)`, `border-color: transparent` for `.site-nav .right .btn.primary`. Brightness +10% on hover.
    - [ ] 5.1o.5 Suppress the legacy `.btn::before` icon decorations (◆ / ⚡ / 📖 / ⭐) on every nav-cta button via `.site-nav .right .btn::before { content: none !important }`. The new-design nav CTAs are clean text — the icon prefix conflicts with the editorial typography.

  - [ ] 5.1p Delta close-out — visual fidelity + a11y + token coverage + DESIGN.md drift. Surfaced by the round-3 5-agent review pass (visual-fidelity / axe-core a11y / token-coverage / DESIGN.md-vs-impl / CSP-SRI) over `cd005b4..HEAD`. CSP/SRI shipped clean (`✓ ship`); the remaining 14 findings split across the four other lenses are tracked here. Each sub-task is one focused commit.

    Critical:
    - [ ] 5.1p.1 Fix `aria-required-children` on the trust-chain `<ol>` (`src/components/TrustChainIsland.tsx:104`). Promoting each `<li>` to `role="button"` strips its implicit `listitem` role, so the parent `<ol role="list">` reports zero `listitem` descendants — AT users hear "list with 0 items" instead of the 5-stage chain. Drop the explicit `role="list"` on the `<ol>` (browser implicit role suffices) AND restructure each stage so `role="button"` lives on an inner `<button>` or `<div>` wrapper, leaving the `<li>` as a real listitem. Re-run `tests/home-axe.spec.ts` (chromium / webkit / Mobile Safari) and confirm zero `aria-required-children` violations.

    Major (visual):
    - [ ] 5.1p.2 Drop the `#hero-h1` rainbow-gradient text + the 8 s `rainbow-shift` keyframes from `public/assets/theme.css:1381-1402`. The `.hero h1.display` rule shipped in 5.1j is dead code today (ID-selector specificity beats class). New-design renders the headline solid `var(--fg)` per `new-design/extracted/src/pages/index.html:924-928`. After this, only `.glow-text` inside the eyebrow pill animates.
    - [ ] 5.1p.3 Switch the hero lede from `class="sub"` (`1.15rem` + literal `#a7b4c7`) to `class="lead"` (20 px + `var(--fg-1)`) per new-design `global.css:234`. The `.lead` rule already exists in `theme.css:710-714`; consumer is `src/pages/index.astro` ~line 105.
    - [ ] 5.1p.4 Hero column gap: scope a `.hero .twocol` (or rename to `.grid-hero`) override with `gap: clamp(56px, 6vw, 80px)`. Current legacy `.twocol { gap: 24px }` is reused for the hero and visibly crowds the trust-chain card against the headline.
    - [ ] 5.1p.5 `.section` rhythm: align `padding-block` to `120px` at ≥1024 px (clamp 64-120 px below) and add `border-top: 1px solid var(--line-soft)` with `:first-of-type` resetting it. Restores the audit-page editorial seam between hero / pillars / writing-strip / on-ramp per new-design `global.css:214-219`. **Land 5.1p.4 before this** — regenerate `home-hero-*-{theme}-{bp}.png` baselines once after 5.1p.5 so the gap + section rhythm shifts get captured in a single baseline pass.
    - [ ] 5.1p.6 Scope the legacy `.btn::before` decorative glyphs (◆ / ⚡ / 📖 / ⭐ at `theme.css:621-670`) so they NO LONGER apply to hero / on-ramp / writing-strip / nav CTAs. Best implementation: rename the legacy contract to `.btn--legacy::before` (or wrap the rules in `:where(:not(.btn--clean))::before`) so new-design surfaces opt out by default. The hero `Open the playground` and `Request design-partner access` buttons currently render with `⚡` and `◆` prefixes the mock has none of.
    - [ ] 5.1p.7 Render the inter-stage 1×10 px connector line that visually stitches the chain (`new-design/extracted/src/pages/index.html:1164-1171`). Implement as `.trust-chain__stage:not(:last-child)::after { content: ""; position: absolute; left: 30px; bottom: -10px; width: 1px; height: 10px; background: var(--accent-dim); box-shadow: 0 0 6px var(--accent); }` and `position: relative` on `.trust-chain__stage`. Without it the chain reads as 5 disconnected cards instead of a stitched flow.

    Major (a11y / contrast):
    - [ ] 5.1p.8 Token-tuning to clear 9 WCAG 1.4.3 contrast failures surfaced by axe (`color-contrast`, impact: serious). Two clusters: `--nd-fg-3` (oklch 0.45 0.008 85) on `--bg` measures 2.56:1 (consumers: `.footer-meta`, `.footer-stack`, `.footer-version`, `.hero__affiliations-label`); `--nd-fg-2` (oklch 0.62 0.008 85) on `--bg-2` measures 4.33:1 (consumers: 5 `.trust-chain__stage-sub` rows). Fix via `--nd-fg-3` → `oklch(0.62 0.008 85)` (matches current `--nd-fg-2`) and `--nd-fg-2` → `oklch(0.70 0.008 85)`, OR restrict each token's allowed consumers to bold/≥18 px contexts (per WCAG 1.4.3 large-text rule). Re-run `tests/home-axe.spec.ts` PLUS a footer-scoped axe sweep on `/vision` + `/writing/welcome` (the footer + on-ramp affiliations reuse `--fg-3`/`--fg-2` everywhere — site-wide regression risk if only `/` is verified) and confirm zero `color-contrast` violations. Regenerate visual baselines after the token shift.

    Major (token coverage):
    - [ ] 5.1p.9 Define the missing `--card-bg` and `--card-stroke` tokens in `public/assets/theme.css` (`:root` block) as `--card-bg: var(--bg-1); --card-stroke: var(--line);`. Consumers: `src/components/FaqItem.astro:21-22`, `src/components/FaqSearch.astro:50-51,85`, `src/pages/faq/index.astro:85,121-122`. Currently undefined, no fallback → cards render with `transparent` background and `currentColor` border (invisible against text). Verified via the round-3 token-coverage agent.
    - [ ] 5.1p.10 Rename 5 `var(--font-mono, ui-monospace, …)` references to `var(--f-mono)`. Consumers: `src/pages/writing/[slug].astro:70,89`, `src/pages/writing/index.astro:65,97`, `src/components/Footer.astro:193` (latter has no fallback so it's silently inheriting the parent family). The intended token (`--f-mono` defined `theme.css:69`) is the project canonical name; `--font-mono` was a pre-USMR draft that never got promoted.

    Major (DESIGN.md drift):
    - [ ] 5.1p.11 Regenerate the `DESIGN.md` frontmatter palette hex values from the §2.1 OKLCH triples via `npm run check:oklch-hex-parity` (or `scripts/oklch-to-hex.mjs`). Current frontmatter ships warm-near-black hex (`bg: "#07090c"`, `bg-1: "#0d1013"`, `accent: "#3ceedd"`, etc.) that fails the `>1 LSB drift per channel` parity gate against the cool-neutral OKLCH (hue 260) actually defined in `theme.css:27-41`. The OKLCH §2 tables are the authoritative canonical source; doc-frontmatter drifted.
    - [ ] 5.1p.12 Update DESIGN.md frontmatter typography section — `display.fontFamily` and `body.fontFamily` currently cite `Sohne, ui-sans-serif`. The §3.1 prose + `theme.css:68-71` ship `--f-display: "Space Grotesk"` + `--f-sans: "Inter Tight"` + `--f-serif: "Fraunces"` + `--f-mono: "JetBrains Mono"`. Replace `Sohne` with the actual `--f-display` / `--f-sans` declarations.

    Minor:
    - [ ] 5.1p.13 Bump `--maxw` from `1200px` to `1240px` per new-design `tokens.css:23`. Update `.container` padding to `padding: 0 var(--gutter, 32px)`. Adds 40 px of horizontal breathing room to KPIs / pillar cards / on-ramp. **Sitewide pixel shift** — every snapshot baseline (home + vision + writing routes) regenerates; budget for it in the same commit.
    - [ ] 5.1p.14 Add a visually-hidden `<h2>` between the hero and the pillar grid — e.g. `<h2 class="sr-only">Platform pillars</h2>` inside `<section id="platform">` so the heading order is h1 → h2 → h3 (current ladder skips h2). axe doesn't enforce this under WCAG 2.1 AA tags (it's `best-practice`), but the WCAG 1.3.1 spirit calls for unbroken hierarchies.
    - [ ] 5.1p.15 Implement OR mark Planned the `.num-h2` primitive (cited 3× in DESIGN.md §1.1.5 + §6.7 + §6.12 — "Class. `.num-h2` with `.num` span. See `public/assets/theme.css`"). The class is not authored in theme.css. Either add the rule per the §6.7 spec or downgrade the doc to a "Planned (Phase 5.x)" tag.
    - [ ] 5.1p.16 Update DESIGN.md §3.4 pointer — currently says "see `src/styles/global.css`" for the `[data-hero-font]` override; that file does not exist. Rules live in `public/assets/theme.css:1815-1834`. Rewrite the pointer.

    Informational (downgraded per review-verification-protocol — net-new code or doc-only drift):
    - [ ] 5.1p.17 Tag DESIGN.md §6.5 "Explain layer" subsection (claims `<TrustChain explain>` boolean prop) and §6.13 (full TrustChainTooltip primitive enumerated as shipped) and §6.11 `tweaks.writing` placement controls as **Planned**. Implementation in `src/components/TrustChainIsland.tsx` accepts no props; no `TrustChainTooltip` file exists; `Tweaks.astro` has no `writing` placement wiring. Either implement (multi-commit feature) or mark Planned. Keep documentation accurate.
    - [ ] 5.1p.18 Add `min-height: 560px` to the `.trust-chain` card per `new-design/extracted/src/pages/index.html:1025` so the card height is stable across all 6 scenarios (without it, the chain visibly pulses on scenario change).
    - [ ] 5.1p.19 Resolve the legacy element-`header` rule at `theme.css:475-482` colliding with `.site-nav` — same specificity, source-order decides. Either scope-rename to `.legacy-header` or remove now that 5.1l ships the new sticky nav.
    - [x] 5.1p.20 Test-coverage close-out from round-5 hunt. (a) `Footer.astro` now lists `/writing` (was dropped in the Phase 4-5 stub) + comment reflects route reality (`/use-cases` / `/standards` still 404 → Phase 5.3/5.4). (b) Theme allow-list pre-paint bootstrap pinned by 3 specs (`?theme=fakevalue`, `?theme=javascript:alert(1)`, positive-control `?theme=midnight`) in `tests/home.spec.ts` — closes the localStorage-XSS-shaped gate. (c) Token paint contracts (`--accent` violet OKLCH band 270-310, `--hero-font` Fraunces stack, primary-CTA non-transparent bg) added under "token paint contracts" describe block. (d) Keyboard focus → decision-claim swap covered (was `.hover()`-only). (e) `tests/header.spec.ts` new file gates sticky + `backdrop-filter` (standard + `-webkit-` reflection) + `color-mix()` translucent bg on chromium / webkit / Mobile Safari structurally — fills the cross-engine-renderer gap that the chromium-only snapshot suite leaves. (f) `tests/oklch-to-hex.test.mjs` self-tests the conversion math powering `check:oklch-hex-parity` (7 known-good fixtures). (g) AGENTS.md / CLAUDE.md "Visual snapshots" line corrected to admit chromium-only reality + reference the structural engine-parity tests that backstop the gap until baseline regen.
    - [ ] 5.1p.21 Broaden `tests/styling-snapshots.spec.ts` engine scope. Drop the `testInfo.project.name !== "chromium"` skip on at least one webkit project AND one Mobile Safari project; regenerate `*-webkit-linux.png` / `*-Mobile-Safari-linux.png` baselines via the workflow's `workflow_dispatch` path. Until this lands, cross-engine pixel deltas (color-mix oklab handling, oklch rounding, backdrop-filter rendering) are guarded only structurally by `tests/header.spec.ts`. Estimate: workflow_dispatch run + 1 commit (baseline PNGs ~ 30-40 files at ~50 KB each).

  - [ ] 5.1q SHIP-READINESS CLOSE-OUT (round-5 review remediations). Land all of 5.1q.1–5.1q.12 before flipping the site to public.
    - [ ] 5.1q.1 Stub `/use-cases` route. Author `src/pages/use-cases/index.astro` that consumes `getEntry('pages','use-cases')` and renders the eyebrow / headline / lede / body MDX (the MDX itself is intentionally short for now — full scenario cards land in Phase 5.3). Closes the round-5 nav-target 404 finding (Header.astro:14 advertises `/use-cases`).
    - [ ] 5.1q.2 Stub `/standards` route. Author `src/pages/standards/index.astro` that consumes `getEntry('pages','standards')` and renders the placeholder body. Full registry-driven sections land in Phase 5.4. Closes the round-5 nav-target 404 finding (Header.astro:15).
    - [ ] 5.1q.3 Add `/bridge   /platform#bridge   301!` to `public/_redirects`. The Header navigates to `/platform#bridge` directly, but external inbound links to `/bridge` (mock + future writeups) need a redirect target rather than a 404. Closes round-5 SEO-major.
    - [ ] 5.1q.4 Add `<section id="bridge" aria-labelledby="bridge-heading">` to `src/pages/platform/index.astro` with the canonical bridge sentence rendered inline (sourced from `platform.mdx` frontmatter via `getEntry('pages','platform')`). Header.astro currently links `/platform#bridge` — without the anchor it lands at the page top. Full Bridge section with variants ships in Phase 5.2.
    - [x] 5.1q.5 Author missing component primitives in `public/assets/theme.css`: `.cluster` (CTA flex wrapper, per new-design global.css:265), `.tag` (uppercase mono pill, global.css:103-113), `.kbd` (keyboard-shortcut chip, global.css:96-101), `.badge` (mono pill, tokens.css:70-76), `.explore-card` / `.explore-grid` (4-up cards, global.css:294-307), `chain-pulse` keyframe (active-stage circle pulse, used by §6.5 Planned 5.1d-idle wiring), `chain-spinner` class + `chain-spin` keyframe (8 px ring rotating @ 0.7 s linear, used by 5.1d-idle "checking…" state). Closes the round-5 component-primitive Major findings.
    - [x] 5.1q.6 Wire keyboard navigation on `.trust-chain__scenarios` per WAI-ARIA tablist pattern: `ArrowLeft` / `ArrowRight` walk between dots; `Home` / `End` jump to first / last. Implementation in `src/components/TrustChainIsland.tsx` via `onKeyDown` on each dot button + `aria-controls` linking each tab to the chain panel. Add Playwright assertion in `tests/home.spec.ts` (chromium / desktop) that ArrowRight from dot 0 focuses dot 1 AND swaps the decision class. Closes round-5 M2.
    - [x] 5.1q.7 Per-writing-post enrichment in `src/components/SeoTags.astro`: accept new prop `type` (`'website'` default, `'article'` opt-in), emit `og:type=article` + Article JSON-LD (datePublished / dateModified / author / image) when `type==='article'`. Wire `src/pages/writing/[slug].astro` to pass `type="article"` plus the post's `cover` / `published` / `updated`. Closes round-5 SEO-major.
    - [x] 5.1q.8 Site-wide `WebSite` and `BreadcrumbList` JSON-LD in `SeoTags.astro`. The WebSite block carries `name`, `url`, `potentialAction.SearchAction` (target = `/search?q={search_term_string}`); the BreadcrumbList walks the path segments to derive crumbs (Home › <Section> › <Page>). Closes round-5 SEO-major.
    - [x] 5.1q.9 Tests: headline split-on-`. ` `<br>` count + eyebrow `&` split `<em class="glow-amp">` count, both gated to chromium. Pin against `home.mdx` current strings; a content-author edit that drops the trailing `.` regresses to a single line and the assertion catches it.
    - [x] 5.1q.10 RSS feed shape test (`tests/writing-feed.spec.ts`): GET `/writing/feed.xml`, assert `application/xml` content-type + `<title>Artagon — Writing</title>` + `<item>` count matches `getCollection('writing', e => !e.data.draft).length`.
    - [x] 5.1q.11 `welcome.mdx` parse test via vitest — explicit Zod-shape assertion (currently only implicit via Playwright build success). Closes the round-5 minor finding.
    - [x] 5.1q.12 `tests/kpis-data.test.mts` mirroring `affiliations-data.test.mts` shape: 4 entries, every entry has non-empty eyebrow / value / detail. Closes the round-5 orphan-test finding (currently `src/data/kpis.ts` has no consumer AND no test).

- [ ] 5.2 `/platform` REDESIGN — port the new-design pillar composition. Decomposed into 5.2.0–5.2.7 below; gate the visible page rewrite (5.2.4) on the foundation pieces (5.2.0–5.2.3).
  - [x] 5.2.0 `src/data/glossary.ts` — port the 60+ glossary entries from `new-design/extracted/src/pages/index.html:439-507` (the canonical `GLOSSARY` map). Typed `GlossaryEntry` interface + `as const satisfies Record<string, GlossaryEntry>` shape. Per resolved Open question 3, must cover every acronym surfaced by Pillars.jsx (~25 minimum) plus the cross-route vocabulary that lands when /use-cases / /bridge / /roadmap ship.
  - [x] 5.2.1 `src/components/Standard.astro` — port the `<T>` JSX wrapper to a token-only static Astro component. `term` prop looks up `lookupGlossary(term)`; renders `<a>` with dotted underline + accent hover. Build-time `console.warn` on missing terms (signals gaps loud). Forced-colors override included.
  - [ ] 5.2.2 `tests/glossary-data.test.mts` — vitest invariant: every entry has non-empty `name`, `href` matches `^https?://`, no duplicate `name` strings (the dual `DID/DIDs`, `PEP/PEPs` plurals are intentional aliases — assert keys differ but the canonical URL matches via a `seen` map).
  - [ ] 5.2.3 `src/data/pillars.ts` extension — augment the existing `Pillar` shape with `tagline: string`, `bullets: readonly string[]`, `specimen: { kind, header, payload }`. Bullet text uses an inline-token convention (e.g. `"<T>OIDC 2.1</T> conformance + <T>GNAP</T>"`) parsed by a small `<TokenizedBullet>` helper that re-renders as `<Standard term="…">…</Standard>` chips. The `Pillar.body` field stays for the existing 3-card consumer.
  - [ ] 5.2.4 `src/components/PillarsIsland.tsx` (React island) — `client:visible`, manual-activation tablist (per resolved Open question 2): roving tabIndex on the 3 tab buttons, ArrowLeft/Right walk focus, Home/End jump, Enter/Space commits the panel swap. `aria-controls`/`aria-labelledby` wiring per WAI-ARIA APG. Cyan top border on active tab.
  - [ ] 5.2.5 `src/pages/platform/index.astro` rewrite — replace the legacy `--brand-teal` gradient hero + 4-card highlights grid with: (a) the canonical "Three pillars, one coherent platform." hero, (b) `<PillarsIsland />` consuming `src/data/pillars.ts`, (c) the `<section id="bridge">` from 5.1q.4 (preserved). Drop the `--brand-violet` use-case sub-grid (Phase 5.3 owns that route). Remove the mobile-fit shim from new-design platform.html:7-22 per resolved Open question 1 — ship a real responsive layout. Hero font: introduce `--f-emphasis` token (`var(--f-serif)` default, `inherit` under non-grotesk hero-font variants) per resolved Open question 5; the italic span consumes `--f-emphasis`, not `--f-serif` directly.
  - [ ] 5.2.6 Specimen content review — every `<Specimen>` `payload` body marked with `<!-- @TODO-IDENTITY-REVIEW: <reviewer> by <date> -->` per resolved Open question 4. Cedar specimen seeded with the safe starter (uses only documented Cedar primitives, no Artagon-internal type names). Identity-team sign-off captured in PR comment before merge. Track via `lint:specimens` (sibling of lint:taglines, separate task 5.2.y).
  - [x] 5.2.7 DESIGN.md updates landed — §3.4 hero-font matrix expanded to 4 × 2 (`--f-display` × `--f-emphasis` audit table per resolved Open question 5); §6.4 Standards chip refreshed (Registry now points at the shipped `src/data/glossary.ts`, A11y / dual-consumer / forced-colors notes added, Status: shipped); §6.16 Pillars (Platform tablist) added (Purpose / Anatomy / Rules / A11y / Status). `lint:design` exits 0 errors. The Playwright `tests/hero-font-matrix.spec.ts` gate moves to a follow-on (gated on the matrix prose landing first, which it now has).
  - [x] 5.2.8 `/bridge` standalone route — ports `new-design/extracted/src/components/Bridge.jsx`. `src/data/bridge.ts` (3 PARTIES tuple + 4 STEPS tuple with `LabelNode` glossary tokens) + `src/components/BridgeFlow.tsx` React island (`client:visible`, 2200 ms auto-cycle, click-commits, `userInteractedRef` freezes cycle on user interaction, skipped under `navigator.webdriver` and reduced-motion) + `src/components/BridgeFlow.css` token-only styles + `src/pages/bridge/index.astro`. Drops the `/bridge → /platform/#bridge 301` redirect from `public/_redirects` (route resolves directly). `Header.astro` Bridge nav link rewired from `/platform#bridge` to `/bridge`. The `<section id="bridge">` anchor on `/platform` from 5.1q.4 stays in place for legacy inbound links. `tests/bridge-data.test.mts` shape invariants. DESIGN.md §6.17 BridgeFlow contract added.
  - [ ] 5.2.x — sibling sub-task: regenerate snapshot baselines (`home-platform-*-{theme}-{bp}.png`) on `workflow_dispatch` after 5.2.5 ships. The mobile-fit-shim removal in 5.2.5 will visibly diff Tablet Safari + Mobile Chrome × 3 baselines. Sibling of 5.1p.21.
  - [ ] 5.2.y — `lint:specimens` ast-grep self-test rule. Fails on a `<Specimen>` `payload` body without an adjacent `@TODO-IDENTITY-REVIEW` inline comment. Test fixtures cover happy path + missing comment + truncated date. Companion node:test self-test for the rule itself.

- [x] 5.3 `/use-cases` ADD — ports `new-design/extracted/src/components/UseCases.jsx`. `src/data/use-cases.ts` (`USE_CASES` 4-tuple of `UseCaseScenario`: csr → specialist → valet → ai, each with metrics 3-tuple + ordered trace whose last line is the PERMIT/DENIED decision) + `src/components/UseCasesIsland.tsx` React island (`client:visible`, vertical-orientation tablist, manual activation via Enter/Space, ArrowUp/ArrowDown/Home/End focus walk, roving tabIndex; `RailTab` subcomponent + decision-line `is-decision` accent) + `src/components/UseCasesIsland.css` (260 px rail + 1fr panel grid, divider-strip metrics, mono trace block, forced-colors override) + `src/pages/use-cases/index.astro` rewritten to mount `<UseCasesIsland client:visible />` (replaces the 5.1q.1 PageLayout stub). `tests/use-cases-data.test.mts` (24 invariants — 4-tuple shape, unique ids/labels, ≥ 3-line trace, decision matches `/PERMIT|DENIED/`, metrics 3-tuple, scenario taxonomy exhaustively covers Human→Human / Human→Machine / Autonomous). DESIGN.md §6.20 UseCasesIsland contract added.
- [x] 5.4 `/standards` ADD — ports `new-design/extracted/src/components/Standards.jsx`. `src/data/standards.ts` (`STANDARDS_GROUPS` 3-tuple + `STANDARDS_BADGES` 4-tuple) + `src/components/StandardsWall.astro` (token-only static — no React island; 3-column grid with mono name + description rows + 4-badge affiliation footer) + `src/pages/standards/index.astro` (replaces the 5.1q.2 PageLayout stub). Item names render as `.standard-chip` links via `lookupGlossary` — coverage ≥ 70% gated by `tests/standards-data.test.mts`. The canonical Pillars vocabulary (OIDC 2.1, GNAP, DPoP, etc.) is fully covered + test-gated. DESIGN.md §6.18 StandardsWall contract added.
- [x] 5.5 `/writing` index REFRESH (Blog alias) — replaces the 5.1q `--brand-teal` stub with the canonical "Blog · Field notes from the Artagon team" composition ported from `new-design/extracted/src/pages/blog.html`. Hero: mono eyebrow + display `<h1>` with serif-italic emphasis on "trust" (consumes `--f-emphasis` token from 5.2.5) + 18 px lede. Post cards: 140 px mono-stacked date column (YYYY / MM · DD) + flex content (display title + lede + mono meta line "Tag · X min read · Author"). Hover tint `color-mix(in oklab, var(--accent) 4%, transparent)` matching the canonical. Reading-time computed at build time at 220 wpm with a 1-minute floor. Primary tag drives the meta-line eyebrow (falls back to "Field notes"). Author byline resolves optionally through the `authors` collection. Empty state if every post is draft. `/blog` + `/blog/` + `/blog/*` → `/writing` 301 redirects added to `public/_redirects` (the deferred follow-up promised in `Header.astro` line 10 from 5.1l). `Header.astro` comment updated to reflect the shipped redirect. DESIGN.md §6.21 Writing-index contract added.
  - [x] 5.5.1 — canonical-fidelity hygiene pass after browser review surfaced duplicate heroes on `/bridge` + `/platform`. (a) `/bridge/index.astro` simplified to mount `<BridgeFlow client:visible />` directly with no surrounding hero — the BridgeFlow island already renders the canonical "Your legacy OIDC app, now cryptographically verified." header per Bridge.jsx, matching `bridge.html` line 768 (`<App><Bridge/></App>`). (b) `/platform/index.astro` simplified to mount `<PillarsIsland client:visible />` only — drops both the `platform-hero` section AND the `<section id="bridge">` 5.1q.4 anchor stub (now redundant since /bridge resolves directly via 5.2.8). Matches `platform.html` line 848 (`<App><Pillars/></App>`). (c) `Header.astro` brand mark replaces the legacy `/icons/icon-64.png` hexagon raster with the canonical inline SVG glyph from `index.html:538-546` (circle + triangle + dot + base line, 22×22, currentColor). Wordmark adjusted to canonical 17 px / weight 500 / -0.01em tracking / `var(--f-sans)` per `index.html:550-554`. DESIGN.md §6.17 BridgeFlow status footnote updated.
  - [x] 5.5.2 — pillars data + home composition aligned to canonical after deeper new-design audit. (a) `src/data/pillars.ts` pillar fields rewritten to match `Pillars.jsx:13-78` exactly: eyebrow values are now the canonical taxonomy labels (`Identity` / `Credentials` / `Authorization`, uppercased at render) instead of protocol-cluster slogans; titles are the canonical brand names (`High-Assurance Identity` / `Decentralized Trust Layer` / `Graph-Native Authorization`) instead of protocol-feature names; bodies match canonical wording. (b) Specimen `header` field repurposed to carry the canonical technical line — Identity is now the JWK JSON `'{"alg":"ES256","typ":"dpop+jwt","jwk":{…}}'` (rendered uppercase via CSS `text-transform`), Credentials gains the `— selective disclosure` suffix, Authorization is `cedar — permit.cedar`. The "DPOP KEY" / "PRESENTED VC (SD-JWT)" / "CEDAR — permit.cedar" labels were rendering as the head of the specimen panel — canonical uses the JSON line directly. (c) `src/components/HomeExplore.astro` ADDED — token-only port of `HomeExplore.jsx` (6-card "Explore" grid: 4 cards `Platform / Bridge / Use cases / Standards` then 2 wide cards `Roadmap / GitHub`). (d) `src/pages/index.astro` restructured to match canonical home composition `<Hero/><Writing/><HomeExplore/><CTA/>` per `index.html:1658` — drops the 3-card pillar overview (canonical home doesn't show one; pillars surface on /platform), inserts `<HomeExplore />` after the writing strip and before the onramp.
  - [x] 5.5.3 — `Footer.astro` columns + brand glyph aligned to canonical (`index.html:605-660`). 4 columns × 5 links each: Platform (Identity / Credentials / Authorization / Bridge / Use cases), Developers (Docs / SDKs / Playground / GitHub / Standards), Company (About / Roadmap / Writing / Security / Contact), Legal (Privacy / Terms / Trust center / DPA / security.txt). External GitHub link gets `target="_blank" rel="noopener noreferrer"`. Brand glyph replaces the legacy `/icons/icon-64.png` raster with the canonical inline SVG (matches Header glyph from 5.5.1) — `currentColor` so it inherits hover-tint via the existing `.footer-wordmark:hover` rule.
  - [x] 5.5.4 — multi-agent tier-1 review fixes. (a) NEW `src/components/ArtagonGlyph.astro` — single-source SVG mark consumed by both Header + Footer (~26 lines verbatim duplication eliminated, matches canonical pattern at `index.html:538-546`). (b) Footer Legal column reverted to canonical `'#'` placeholders for Terms / Trust center / DPA / Sub-processors — the 5.5.3 mappings were silently misdirecting users (clicking "Terms" went to `/privacy`, etc.) per silent-failure-hunter Major. (c) JSDoc drift fixed: `Specimen.header` (now describes per-kind shape spectrum), `Pillar.eyebrow` (canonical taxonomy + uppercased-at-render rule). (d) `HomeExplore.astro` external `rel` → `noopener noreferrer` (matches Footer convention).
  - [x] 5.5.7 — CI fix + HomeExplore data extraction + author rename. (a) **CI failure fix** — `tests/home.spec.ts:33-35` was asserting `#pillar-identity / #pillar-credentials / #pillar-authorization` cards on the home page; the 3-card pillar grid was dropped from home in 5.5.2 (canonical home composes `<Hero/><Writing/><HomeExplore/><CTA/>` only). Updated test to assert the canonical sections instead: `#playground` (HomeExplore section), `.home-explore__card` count = 6, `#writing` writing strip, `#get-started` on-ramp. (b) **HomeExplore data extracted** to `src/data/home-explore.ts` — `PRIMARY` (4-tuple) + `SECONDARY` (2-tuple) + `isExternal()` predicate. `HomeExplore.astro` now imports from the registry. Mirrors canonical `HomeExplore.jsx`. (c) **NEW `tests/home-explore-data.test.mts`** (~27 invariants — tuple shape, idx ordering, content quality, href shape, canonical destinations, isExternal predicate). (d) **Author rename** — `src/content/authors/giedrius-trumpickas.mdx` → `writing-blog.mdx` per user direction. Slug `"trumpyla"`. All 3 writing posts updated. Vitest 167 → 194.
  - [x] 5.5.9 — visible-fidelity batch from "half not converted" + "blue areas" + footer-not-aligned user feedback. (a) **Body background flattened** — dropped the 5.1-era 3-stack `radial-gradient(violet 25%) + radial-gradient(teal 18%) + linear-gradient(--bg-alt → --bg)` per canonical `global.css:68-75` (`body { background: var(--bg) }` flat). The radials were the "blue areas" the user pointed at — top-left + top-right blue/violet auroras on every page above the fold. The `body::before` 88×88 grid overlay stays. (b) **Footer column headings** rewired to canonical font/size/color — was rendering in `var(--f-display)` (Space Grotesk) because the 5.5.5 bare-`h3` rule cascaded through (`.footer-col-heading` didn't set `font-family`). Now matches `index.html:618` exactly: `var(--f-mono)` 10.5px / weight 500 / `--fg-3` color / 0.14em letter-spacing / 16px margin-bottom. Result: PLATFORM/DEVELOPERS/COMPANY/LEGAL render identical mono-uppercase. (c) **Subpage hero h2 size sweep** — bumped 4 components (PillarsIsland, UseCasesIsland, StandardsWall, RoadmapTimeline + HomeExplore) from `clamp(2rem, 4vw, 3rem) / line-height 1.15` (32-48px loose) to canonical `clamp(36px, 4vw, 60px) / line-height 1.02` per `global.css:230`. Gives /platform / /use-cases / /standards / /roadmap / home-explore a ~25% bigger, tighter hero h2 matching mock weight. (d) **`.btn.primary` solid-fill ascended to global** — was scoped to `.site-nav .right` only (Header.astro), so Hero + onRamp primary CTAs ("Request design-partner access", "Apply as a design partner") fell through to the legacy `.btn` translucent-teal-mix base and rendered as ghost pills. Added the canonical `var(--accent)` solid + `var(--accent-ink)` ink at theme.css root scope. (a) NEW `tests/footer.spec.ts` (Playwright, 6 cases) — 4×5 column invariants; canonical headings; column composition; external GitHub `rel="noopener noreferrer"`; brand glyph inline SVG. (b) NEW `tests/new-routes.spec.ts` (Playwright, 5 cases) — smoke specs for /bridge / /use-cases / /standards / /roadmap / /platform. (c) NEW `tests/redirects.test.mts` (vitest, 7 cases) — parses `public/_redirects` directly; gates `/blog` 301 contract + drift guard against `/bridge` redirect regression. (d) **Design-drift CI fix** — `--gutter` token allow-listed in `docs/design-md.md` §6.26. Vitest 194 → 201; Playwright +11 cases.
  - [x] 5.5.6 — gap-analysis remediation batch + tests + DESIGN.md sync. Code: (a) Hero CTA hrefs flipped from `/get-started` / `/play` (multi-page) to canonical anchor jumps `#get-started` / `#playground` per `Hero.jsx:166-167`; HomeExplore section's `id` renamed `explore` → `playground` to provide the secondary anchor target. (b) `.display` tracking-cascade bug fix in theme.css — bare `<h1>` and `.display` now read `var(--display-tracking, var(--tracking-h1))` so the per-face tracking documented in §3.4 actually applies under `[data-hero-font]` toggles (was reading the global `--tracking-h1` directly, ignoring per-face overrides). (c) `[data-density]` token blocks added to theme.css — `--gutter` defaults to 32 px (`comfortable`), `dense` → 20 px, `roomy` → 44 px per canonical `tokens.css:24-25` + `global.css:64-65`. DESIGN.md §3.3/§4 had documented the attribute since 5.1 but the override blocks were never shipped. (d) Authors collection populated — `src/content/authors/giedrius-trumpickas.mdx` (canonical byline per `Cta.jsx` writing-widget references). All 3 writing posts (welcome / compounding-trust-chain / bridge-strategy) now carry `author: giedrius-trumpickas`. Eliminates the "authors collection does not exist or is empty" build warning. Tests: (e) NEW `tests/pillars-data.test.mts` (17 invariants — 3-tuple shape, eyebrow taxonomy, brand-name titles, num prefixes, glossary chip resolution, specimen header per-kind shape) — closes the long-standing "file referenced in src/data/pillars.ts:9 but missing" gap. (f) NEW `tests/writing-collection.test.mts` (15 invariants — 3-post minimum, canonical filenames present, per-post frontmatter required-fields + published-date-valid + tags-array + author-resolution + non-draft) — gates new posts at vitest time. (g) NEW `tests/contrast-tokens.test.mts` (13 WCAG AA assertions — 6 normal-text pairs ≥ 4.5:1 + 7 large-text/non-text pairs ≥ 3:1) — closes the CLAUDE.md phantom CI gate that referenced a non-existent contrast test. Uses the existing `oklchToHex` utility so any drift in OKLCH-conversion math fails this AND `check:oklch-hex-parity` in the same diff. Vitest now 167/167 (was 115). DESIGN.md: (h) §6.5 Trust chain — stale "plays once on first mount only" claim corrected to "plays on every scenario change; auto-cycle continues after click" per 5.5.5 freeze removal. (i) §6.16 Pillars Specimen header — stale "spec.kind label uppercase" claim corrected to per-kind free-form (JSON / sentence / lowercase mono filename); CSS uppercases authored values. (j) §6.17 BridgeFlow — two stale claims about `userInteractedRef` permanent-freeze deleted/corrected. (k) §6.4 Standards chip — "Two consumers" updated to "Four consumers" (Standard.astro + PillarsIsland StandardChip + BridgeFlow StandardChip + StandardsWall direct .standard-chip class). (l) NEW §6.22 ArtagonGlyph (shared brand mark — single-source SVG consumed by Header + Footer; 5.5.4 origin). (m) NEW §6.23 HomeExplore (6-card grid; 4 + 2 wide split; cyan hover tint; serves as `#playground` anchor target post-5.5.6). (n) §3.2 type scale — bare-element rules note added ("type scale applied at <h1>/<h2>/<h3> selector since 5.5.5; .display utility survives as opt-in"). (o) §4 Layout — `.serif` and `.display` token-only utility classes documented; canonical `[data-density]` density-variant block + `--gutter` token added. (p) §7.1 — "Serif italic emphasis in MDX frontmatter" do/don't subsection added documenting the `*marker*` split-on-asterisk pattern (5.5.5).
  - [x] 5.5.16 — large canonical-fidelity batch surfaced by user side-by-side comparison ("looks ugly not the same" + "different fonts styles"). Multi-agent delta scan across Hero/TrustChain · Pillars/Bridge/Standards/UseCases/Roadmap · Header/Footer/Glossary · Writing post detail · Theme tokens · onRamp/Get-started yielded 1 critical + 14 Major + 7 Minor items. **Code (9 batches):** (a) **CRITICAL — webfonts** — `src/layouts/BaseLayout.astro` ships zero webfonts → every `--f-*` token fell back to system-ui (San Francisco on macOS, Segoe UI on Windows). Added canonical Google-Fonts preconnect + 6-family `<link rel="stylesheet">` per new-design/extracted/src/pages/index.html. Body `font:` shorthand in theme.css that pinned `system-ui, -apple-system, …` was split into individual properties so `--f-sans` (Inter Tight) actually takes effect. (b) **Header** — `NAV_LINKS` trimmed from 6 → 4 (Platform · Use cases · Standards · Writing) per canonical BaseLayout.jsx:203-208; "Blog" relabeled "Writing". GitHub moved out of the link list into a 34×34 rounded-square icon button in `.right` per canonical BaseLayout.jsx:220-238 (full inline SVG mark, hover `border-color: var(--fg-3)`, focus-visible accent outline). (c) **Footer** — flat 5-col grid `1.4fr repeat(4, 1fr)` per canonical BaseLayout.jsx:283 (was `220px 1fr` + nested 4-col); ThemeToggle dropped from brand col (canonical Footer fn lines 284-288 has wordmark + 28ch positioning blurb only); positioning blurb font-size `0.82rem` → `13px` + max-width `200px` → `28ch`. (d) **onRamp** — `.onramp__grid` `1.6fr 1fr` → `1.2fr 1fr` per Cta.jsx:25; NEW `.onramp__headline-emphasis` rule with `font-family: var(--f-emphasis, var(--f-serif))` + `font-style: italic` (was missing — italic on "next" never rendered); 4 contact-card colors corrected — title fg-2→fg-3, dt fg-2→fg-3, dd fg→fg-1, footnote fg-2→fg-3. (e) **TrustChainIsland** — evaluating-state reasoning copy `// ${step}/${STAGES.length} stages evaluated` → canonical `// chain composes proofs; any stage can halt` (the counter-form duplicated the claim line). Hero meta-strip `font-family: ui-monospace, Menlo, Consolas, monospace` → `var(--f-mono)` (token consistency). (f) **Theme tokens** — `.btn` `padding: 0.5rem 0.85rem` / `border-radius: 10px` / `gap: 0.4rem` / `font-size: 0.875rem` → canonical `12px 18px` / `8px` / `10px` / `14px` per global.css:236-244; `.wrap` `padding: 0 20px` → `0 var(--gutter, 20px)` (density-aware); `.explore-card` `padding: 20px` / `gap: 12px` / no min-height → canonical `28px 24px 24px` / `14px` / `min-height: 220px` per global.css:296-303; `.tag` padding `4 8` → `4 10 4 8` + `.tag .dot` gained `box-shadow: 0 0 12px var(--accent)`. (g) **Pillars** — Authorization Cedar specimen restored to canonical Pillars.jsx:76-77 (`principal in Group::"csr_team"` + `action == Action::"view_account"` + `resource is Account` + `context.delegation.vc.valid` predicates). The pre-fix generic `principal/action/resource` form lost the VC-delegation graph semantics that made the Authorization pillar visually distinct from a stock RBAC example. (h) **Writing post detail** (8 primitive restyles per blog.html:356-453) — h2 `margin-top` 56→64 + clamp `(24,32)/1.15` → `(26,34)/1.12`; h2 `.num` `vertical-align: middle` inline → `display: block` + `margin-bottom: 8px` + tracking `0.12em → 0.14em`; inline `<code>` accent-tinted bg → neutral `var(--bg-1)` + `var(--line-soft)`; `.pull` left-stripe-2px → full `1px solid var(--line)` + 36px pad + `clamp(24, 32)/1.3`; `.chain-row` grid `28px 1fr auto` border-top divider → `40px 1fr auto` + per-row `1px solid var(--line-soft)` + `var(--bg)` boxed-row; CTA cards `20×22` + `min-height: 156` + `var(--line-soft)` → `26px` + no min-height + `var(--line)`; `.compare .col` padding 24 → 22. (i) **Minor sweep** — `.bridge-flow__title` font-size override removed (lets the global h2 type scale apply); `PillarsIsland.css:65` tablet-breakpoint margin-bottom 48→40; `faq/index.astro:71` `btn solid` → `btn primary`. **Tests:** NEW `tests/webfonts.spec.ts` (2 cases — preconnect + 6-family `<link>` + h1.fontFamily resolves to Space Grotesk after `document.fonts.ready`). Expanded `tests/footer.spec.ts` +2 (canonical `1.4fr repeat(4,1fr)` grid template + ThemeToggle absence in brand col). Expanded `tests/header.spec.ts` +2 (canonical 4-link list + GitHub icon button in `.right`). Vitest 204/204; Playwright +6 cases; build 23 routes clean; lint:design 0 errors. **DESIGN.md:** §3.1 (NEW webfont-loading subsection); §6.1 (4-link rule + 34×34 GitHub icon button); §6.2 (flat 5-col grid + alignment override + ThemeToggle removal); §6.6 (canonical `.explore-card` sizing); §6.8 (canonical `.btn` token contract + `.wrap` density-aware gutter); §6.12 (post-body primitives table comparing canonical vs pre-fix values).
  - [x] 5.5.16-loop (iters 2-80, ~74 follow-on commits pt2-pt77) — Ralph-loop deep-audit pass following the 5.5.16 batch. Each iteration spawned a delta scan against `new-design/extracted/` and shipped a single canonical-fidelity fix as a separate commit. **Cumulative scope:** (a) `pt2` legacy `.btn::before "◆/📖/⚡/⭐"` glyph decorations dropped globally (saved ~63 lines + ~60 lines of surface-scoped suppressions). (b) `pt3` mobile-nav contract gate (5.5.11 hamburger pattern). (c) `pt4` webfonts spec pinned chromium (engine-agnostic contract; webkit IPv6-only dev-server flake). (d) `pt5` CTA routing — Playground/Request access → `/#playground`/`/#get-started` anchors (was `/play` shim + `/get-started` route); Footer Developers col CLI (was Playground). (e) `pt6` test gates for pt5 contracts. (f) `pt7` skip-link canonical-token migration + visibility test. (g) `pt8` orphan MissionSection.astro removed (192 lines, zero consumers). (h) `pt9` post-footer CTA card border-radius 12 → 14. (i) `pt10` Hero lede bolds full predicate phrases (`claim is proven`/`token is bound`/`decision is traceable` not just last word). (j) `pt11` blog-hero h1/byline/eyebrow/footer-padding canonical (5 fine drifts). (k) `pt12` chain-fig + chain-row canonical (5 drifts including grid layout). (l) `pt13` chain-row .num circle node (was flat text → 28×28 accent-bordered circle) + connector line ::before. (m) `pt14` home writing-strip 3-up canonical (8 drifts). (n) `pt15` HomeExplore eyebrow → canonical `.eyebrow` styling. (o) `pt16` canonicalize 6 eyebrow rules (.eyebrow + 5 island scoped variants — every section break now has 18px accent dash). (p) `pt17` 6 hardcoded mono chains in TrustChainIsland.css → `var(--f-mono)`; UseCases panel margins. (q) `pt18` Pillars specimen + tagline canonical px (3 drifts including 28 px tagline margin-bottom that was missing). (r) `pt19-22` 5 components rem→fixed-px sweep (BridgeFlow party + steps; UseCases tabs + metrics; RoadmapTimeline + StandardsWall; TrustChainIsland 8 sizes). (s) `pt23-24` OnRamp canonical token alignment (10+ drifts including h2 ceiling 60→76, card padding 32→72/64, headline-emphasis missing italic rule, contact card colors fg-2→fg-3 across 4 properties). (t) `pt25` hero + .section padding canonical (78 px hero bottom shortfall + section-pad floor 64→80). (u) `pt26` hero .lead margin-bottom + .actions cluster gap canonical. (v) `pt27` `.lead` color `var(--fg-2)` → `var(--fg-1)` (canonical brighter editorial weight). (w) `pt28` legacy tokens (--text/--muted/--border/--brand-teal/--bg-alt/--surface) aliased to canonical (5.1h plan finally landed). (x) `pt29-30` `.btn` base + hover canonical (drop teal-tint border/bg, drop backdrop-filter + box-shadow). (y) `pt31` h1 weight 500 → 400 (canonical regular); `.container` padding density-aware. (z) `pt32` Pillars tabs/body/bullets canonical px (4 drifts). (aa) `pt33` writing-strip head — canonical "Writing" eyebrow + serif-italic "the team" + grid layout. (bb) `pt34` 5 section ledes 17px → 20px canonical. (cc) `pt35` writing-strip h2 ceiling 40 → 60 canonical. (dd) `pt36` writing-strip**more + footer base + meta canonical px. (ee) `pt37` post-body .compare label margin + .pull attribution. (ff) `pt38` hero affiliations row → canonical StandardChip pills with cyan dot (was dotted-underline links). (gg) `pt39` footer-meta justify-content space-between (was clustered left). (hh) `pt40` trust-chain card padding 28 + head separator var(--line-soft). (ii) `pt41` nav links margin-left 24 px (canonical breathing). (jj) `pt42` `.display` utility drops font-weight (specificity bug — was beating canonical h1 weight 400). (kk) `pt43` writing-strip h2 ceiling 40 → 60 canonical. (ll) `pt44` writing index hero — canonical .eyebrow + h1 margin-top 16 (40 px eyebrow→h1 separation restored). (mm) `pt45` get-started + 404 ledes 17 → 20px. (nn) `pt46` trust-chain head font-size canonical 11. (oo) `pt47` Footer padding + gap canonical px. (pp) `pt48` writing-strip + onramp sections drop scoped padding (72 px shortfall — biggest vertical-rhythm fix). (qq) `pt49` 6 island sections padding-block floor 64 → 80 canonical. (rr) `pt50` trust-chain scenario gap 12 → 8 + footer wordmark canonical. (ss) `pt51` bridge-flow border-block → border-top (drop redundant bottom). (tt) `pt52` writing-strip cta — split label/arrow span for flex-gap to apply. (uu) `pt53-54` Pillars + UseCases panel-prose drop flex-gap (double-counting bug). (vv) `pt55-57` font-family fallbacks canonicalized (token-chain trust). (ww) `pt58` orphan CSS removed — domain-highlight + pillar-card + pill-number (~115 lines). (xx) `pt59` hero meta-strip drop redundant padding-block (32 px vertical). (yy) `pt60` DESIGN.md sync — 5 new blocks documenting the loop's contracts. (zz) `pt61` NEW `tests/canonical-typography.spec.ts` (6 cases gating h1 weight 400 / .lead 20 / eyebrow accent dash 18px / hero padding 80/120 / writing-strip section padding / .actions gap 10) + caught .blog-hero**title weight 500 → 400 regression. **Cumulative metrics:** ~60 commits shipped; ~50 rem→px conversions across ~14 files; ~430 lines of legacy CSS / orphan components removed; vitest 204/204 stable; +12 Playwright test cases (header / footer / webfonts / canonical-typography); build 23 routes clean; lint:design 0 errors; lint:tokens 0 raw colors. **DESIGN.md:** §3.1 (NEW font-family token contract + h1 weight cascade + section ledes); §4 (NEW section vertical rhythm + flex-gap anti-pattern); §6.3 (NEW section eyebrow vs glow-tag distinction with 8-surface contract). **Iter 64-68 follow-on:** `pt62` tasks.md sync (this row). `pt63` canonical-typography spec +3 cases (footer-meta justify-content / onRamp h2 ceiling 76 / .trust-chain padding 28). `pt64` trust-chain decision card color `var(--fg)` → `var(--fg-1)` (canonical Hero.jsx:399 — accent-tinted card surface needs the brighter editorial weight). `pt65` post-body inline `<code>` color `var(--fg)` → `var(--accent)` (canonical post-bridge-strategy.html:374 — inline-code chip reads as cross-reference signal, was collapsing into prose) + canonical-typography spec +1 case anchored to `/writing/bridge-strategy`. `pt66` post-body `.pull` / `<blockquote>` background `accent-tinted bg-1 → bg-1` → `bg-1 → bg`, border-radius 8 → 14, margin 36 → 56, + `max-width: none` (canonical post-bridge-strategy.html:378-387 — pull-quote shares card-on-canvas surface treatment with `.chain-fig`; pre-fix accent-tint baseline + radius 8 read as a sidebar callout, not the editorial pause-block) + canonical-typography spec +1 case targeting `<blockquote>` border-radius. `pt67` post-footer cta-card hover border `var(--accent-dim)` → `var(--accent)` + transition simplified from 3 props × 0.18s (with dead `transform`) → 2 props × 0.15s (canonical post-bridge-strategy.html:456-458). `.chain-caption` letter-spacing 0.06em → 0.08em + drop center text-align (canonical post-bridge-strategy.html:423-426 — aligns with eyebrow / footer-tags / chain-row .sub mono micro-text scale). +1 canonical-typography case (12 total) parsing the cta-card :hover rule directly from the stylesheet (Playwright `.hover()` + `getComputedStyle` race). `pt68` orphan `gap: 1rem;` declaration + stray `}` after `.onramp__row` rule in `src/pages/index.astro` (lines 566-573 pre-fix) — Astro's CSS parser silently dropped the orphan and tolerated the stray brace, so the build passed but the dead code was a latent landmine for any future strict-parsing tool. New `tests/lint-style-block-balance.test.mts` (47 cases) walks every `.astro` file's `<style>` blocks and asserts brace balance after stripping comments — would have caught this at commit time. `pt69` `/get-started .cta-section` padding-block `clamp(64-120)` symmetric → `padding-top: clamp(80-120/8vw)` + `padding-bottom: 80px` flat (canonical Cta.jsx:9 — section is `.section` class with inline `paddingBottom: 80` lock; pt25/pt48/pt49 had already canonicalized the 80-floor across other sections, but get-started uses scoped `.cta-section` so it was missed). +1 canonical-typography case (13 total) gating top within [80, 120] + bottom flat 80. `pt70` 404 page eyebrow — pt15/pt16 8-surface eyebrow sweep missed `.not-found__eyebrow` (404 was added in 5.5.14, after that sweep). Shipped 11/fg-3/0.12em with no accent dash; canonicalized to 12/fg-2/0.1em + 18 px ::before accent dash, bringing total surfaces to 8. +1 canonical-typography case (14 total) gates font-size 12, computed letter-spacing 1.2 px (= 0.1em × 12), dash width 18. `pt71` orphan `src/layouts/PageLayout.astro` removed (87 lines) — file was the 5.1q-era hero scaffold for `/use-cases` + `/standards`; both routes were re-platformed to canonical island compositions in 5.3 / 5.4 and the layout had **zero consumers** since (only stale comments in those two pages referenced it). Carried 4 sub-canonical primitives that could mislead future contributors: `.page__eyebrow` 0.08em+0.75rem with no accent dash, `.page__hero h1` clamp(2-3rem) (canonical hero h1 clamp 44-84), `.page__lede` fg-2 1.15rem (canonical 20/fg-1/1.5), `.page` padding-block var(--space-12)=48 (canonical clamp(80, 8vw, 120)). Comments in use-cases + standards routes also cleaned of "Replaces the PageLayout stub" language now that the file is gone. Brace-balance walker drops 1 case (47 → 46). **Final cumulative:** 68 commits · vitest 250/250 · canonical-typography 14/14 · webfonts 2/2 · footer 9/9 · header 8/8 · build 23 routes clean. Loop has now removed ~520 lines of dead code (pt8 MissionSection 192 + 5.7 RoadmapPhaseCard/RoadmapTable + pt58 orphan island CSS 115 + pt71 PageLayout 87).

`pt72` three more orphan components removed: `src/components/Difference.astro` (46 lines, old `.diff-section` 3-card composition referencing classes no longer in theme.css), `src/components/Logo.astro` (19 lines, superseded by `ArtagonGlyph` in Header/Footer), `src/components/LogoVariants.astro` (108 lines, variant resolver for the logos package, never imported). Total this iter: 173 lines. **Cumulative dead-code removed across the loop: ~693 lines.** Brace-balance walker drops 3 cases (46 → 43; total vitest 250 → 247). `pt73` orphan `src/data/kpis.ts` (37 lines) + `tests/kpis-data.test.mts` (49 lines, 6 cases) removed — 5.1n preserved the registry expecting Phase 5.2 `/platform` redesign to re-render the legacy KPI band, but 5.2 canonicalized `/platform` to PillarsIsland with no KPI consumer. Comment in `src/data/roadmap.ts` already flagged the field as "never consumed downstream". Test was a "preserve" gate that pinned the contract awaiting a consumer that never materialized. Total this iter: 86 lines + 6 vitest cases. **Cumulative dead-code removed across the loop: ~779 lines.** `pt74` 3 orphan / token-violating rules in `public/assets/theme.css` cleaned up: (a) `.kpi` grid rule (11 lines) — leftover from 5.1n KPI band that was canonicalized away; data registry deleted in pt73, the orphan CSS rule was the missing twin. (b) `.badges` flex rule (5 lines) — zero consumers in src/. (c) Legacy bare `footer { ... }` selector (5 lines) — fully overridden by `.site-footer` in Footer.astro; carried hardcoded `rgba(148, 163, 184, 0.12)` border + `#9fb1c8` color (token violations skipped by lint:tokens because lint walks src/ only). (d) `.discrete` (3 consumers — ShimPage / /console / /docs) had hardcoded `#8fa0b8` color → canonicalized to `var(--fg-2)` so theme/accent overrides flow through to placeholder copy. Total this iter: ~24 lines + 1 token violation fixed. **Cumulative dead-code removed across the loop: ~803 lines.** `pt75` pre-USMR `.cta` / `.cta-large` / `.cta-section` rules removed (~80 lines) — `.cta` + `.cta-large` had ZERO consumers in src/ (every CTA uses canonical `.btn` from §6.8 instead). Carried hardcoded `#041016` + `rgba(255, 255, 255, 0.2)` / `rgba(255, 255, 255, 0.3)` rgbas (token violations skipped by `lint:tokens` because lint walks src/ only — same gap caught in pt74). Hardcoded fallbacks `var(--primary, #22e3c5)` + `var(--brand-sky, #38bdf8)` pinned a violet/cyan palette regardless of theme. `::before "→"` glyph injection would have collided with canonical `.btn .arr` span on any future `class="btn cta"` element. `.cta-section` was active on /get-started ONLY: the scoped /get-started rule overrode padding only (pt69), so the global teal/violet color-mix gradient + double border ran on top of the canonical bg-1 → bg fade `.cta-card` — visually clashed with canonical Cta.jsx (which wraps with bare `.section` class, no colored backdrop). Removing the global rule lets the canonical fade dominate uncluttered. Total this iter: ~80 lines (3 token violations fixed). **Cumulative dead-code removed across the loop: ~883 lines.** `pt76` `lint:tokens` extended to scan `public/assets/theme.css` with a token-aware filter (instead of the pre-pt76 whole-file allowlist that pt74/pt75 found masking class-rule violations). Implementation: raw colors on lines containing a `--token:` declaration are allowed (legitimate token definitions); raw colors anywhere else are violations; CSS comments stripped before scan; `var(--token, FALLBACK)` exempted; per-line `/* lint-tokens: ok */` marker for compositing primitives + shimmer gradients. The extended linter caught 16 latent violations on first run; pt76 resolved them via: (a) delete pre-USMR `.brand` / `nav ul` / `nav a` / `nav a:hover` (~22 lines, fully overridden by Header.astro / Footer.astro scoped styles); (b) delete orphan `.sub` rule (4 lines, hardcoded `#a7b4c7`); (c) tokenize 4 `.ui-*` `color: #fff` → `var(--accent-ink)` on `var(--brand-teal)` backgrounds; (d) tokenize `.hero-art border` rgba → `var(--line-soft)`; (e) tokenize `.site-footer small color` → `var(--fg-3)`; (f) add `/* lint-tokens: ok */` markers on 6 mask + glow-tag + glow-text gradient stops (pure-white/black anchors that would lose semantic meaning if tokenized — they're compositing/specular primitives, not theme colors). Total this iter: ~26 lines orphan removed + 7 tokenizations + 6 allow markers + permanent linter improvement. **Cumulative dead-code removed across the loop: ~909 lines.** `pt77` lint:tokens scanner refactored from line-based to character-position-based + 4 new node:test cases gating pt76 behavior. Caught a regression in the existing test suite: pre-pt76 fixture cram-coded multiple `--token:` decls on a single line; pt76 line-based filter saw the line as not starting with `--`, flagging the legitimate token defs. New scanner walks back from each color match to the nearest `{` or `;` and checks for `--name:` — handles one-decl-per-line, multi-decl-per-line, and multi-line shadow values uniformly. New tests gate: token defs allowed (1 decl/line + multi decl/line); class-rule raw colors flagged; `var(--token, FALLBACK)` allowed; `/* lint-tokens: ok */` marker exempts gradient stops. node:test count 22 → 26. **Final cumulative:** 74 commits · vitest 241/241 · node:test 26/26 (+4) · canonical-typography 14/14 · webfonts 2/2 · footer 9/9 · header 8/8 · build 23 routes clean.

- [x] 5.5.5 — second-round canonical-fidelity batch from gap-analysis review. (a) **Global type tokens** in `public/assets/theme.css` — added bare `<h1>` / `<h2>` / `<h3>` rules consuming `--fs-h1/h2/h3 + --lh-* + --tracking-*` tokens (was previously only inline on home; non-hero pages rendered ~32 px browser default). Added `.wrap` (canonical alias for `.container`), `.serif`, `.display` utility classes at root scope (per `global.css:43-47, :95, :212`). Updated `body` with `font-feature-settings: "ss01", "cv11"` for Inter Tight stylistic-sets (per DESIGN.md §3.1). Reset `.lead` from `clamp(1rem, ..., 1.25rem)` to flat `var(--fs-lead)` 20 px per canonical `global.css:234`. (b) **Hero meta-strip relocation** — moved inside the hero `<section>` from its own `.container` per canonical Hero.jsx:124-141. (c) **onRamp serif emphasis** — `home.mdx onRamp.headline: "Build with the *next* generation of identity."` + index.astro renderer splits on `*marker*` mirroring the existing eyebrow-amp pattern; even-index segments plain text, odd-index `<em class="serif">` for fraunces italic. (d) **Trust-chain + Bridge auto-cycle freeze removed** — `userInteractedRef` permanent latch dropped from both `TrustChainIsland.tsx` and `BridgeFlow.tsx` per canonical `Hero.jsx:116-121` and `Bridge.jsx:16-19` (auto-cycle continues after user click; clicking only resets the active scenario / step, doesn't stop the timer). (e) **Writing hero padding** — top `clamp(56px, 7vw, 72px)` + bottom `8px` per canonical `blog.html:823` (was `40-56px` bottom, creating visible gap drift). (f) **Two missing blog posts** ported as MDX: `src/content/writing/compounding-trust-chain.mdx` (4-min read, Engineering / Authorization / Cryptography tags) + `src/content/writing/bridge-strategy.mdx` (4-min read, Strategy / VCs / OIDC / Migration tags). Build now ships 23 routes (was 21).
- [ ] 5.6 `/writing/[slug]` ADD: breadcrumb, TOC sidebar if ≥ 3 `<h2>`, related posts (2-up), RSS CTA.
- [x] 5.7 `/roadmap` REDESIGN — ports `new-design/extracted/src/components/Roadmap.jsx`. `src/data/roadmap.ts` extended (5.1c-era `Phase` shape replaced by the canonical `RoadmapPhase` 5-tuple + `ROADMAP_STATUS` map; `kpis[]` field dropped as never-consumed; `id` field preserved for `/roadmap#v3`-style deep-link compat) + `src/components/RoadmapTimeline.astro` (token-only static — 5-card horizontal grid with colored status dots, mono version prefix + time band, scope `<ul>`s) + `src/pages/roadmap/index.astro` rewritten to mount `<RoadmapTimeline />` (drops the cards/table view-toggle UX from the 5.1c stub). Orphan files removed: `src/components/RoadmapPhaseCard.astro`, `src/components/RoadmapTable.astro`, `public/assets/roadmap.css`. `tests/roadmap-data.test.mts` (7 invariants — 5 phases V1-V5 in canonical order, monotonic status progression, every status resolves in the meta map). DESIGN.md §6.19 RoadmapTimeline contract added. Latest-update card reused on home is deferred to a follow-on task (5.7.x) — not part of this commit.

## Phase 6 — Standards registry (site-standards-registry)

- [ ] 6.1 `src/data/standards.ts` typed registry with initial 8 entries (IETF GNAP, OpenID OID4VC, FIDO2, W3C DIDs, W3C VCs, NIST 800-63, eIDAS 2, RFC 9449 DPoP) + versions + summaries + longSummaries + extends/uses graph + `lastVerified?` ISO date.
- [ ] 6.2 `StandardChip.astro`: mono label, external icon, `target="_blank" rel="noopener noreferrer"`, desktop hover + mobile-visible caption via `@media (hover: none)`, ≥ 44 px tap target via hit-area padding.
- [ ] 6.3 `StandardsRow.astro`: `<ul role="list">` render.
- [ ] 6.4 `/standards` page consumes the registry as the single source.
- [ ] 6.5 Hero + footer consume the registry — no hand-rolled standards links.
- [ ] 6.6 `scripts/lint-standards.mjs` (AST-aware `.astro` / `.mdx` / `.ts`; skips code fences, comments, frontmatter; honors `// lint-standards:allow` pragma).
- [ ] 6.7 Configure `lychee.toml` weekly scheduled job for `STANDARDS[*].href`; exclude from per-PR Lychee.

## Phase 7 — Bridge story (site-bridge-story)

- [ ] 7.1 Canonical `bridge.sentence` + `bridge.variants[]` allow-list in `platform.mdx` frontmatter.
- [ ] 7.2 `/platform#bridge` section + `BridgeFlow.astro` SVG diagram with `role="img"`, `<title>`, `<desc>`; semantic emphasis (not color-only); `forced-colors` mode tested.
- [ ] 7.3 `scripts/lint-bridge.mjs` (AST-aware, phrase-count heuristic, allow-list).
- [ ] 7.4 Deploy-host redirect: `/bridge` (and case/trailing-slash variants) → `/platform#bridge` (301). Add `public/_redirects`.
- [ ] 7.5 `/get-started#bridge` onboarding path + curl example (Phase 5 dependency).
- [ ] 7.6 `tests/bridge.spec.ts`: canonical sentence appears exactly once on `/platform`; never verbatim elsewhere; 301 verified for `/bridge` variants.

## Phase 8 — Mobile layout (site-mobile-layout)

- [ ] 8.1 Remove global `transform: scale()` fallback from any prototype page that still uses it (under `new-design/extracted/`).
- [ ] 8.2 Implement `TwoCol.astro`, `ThreeCol.astro`, `CodePair.astro`, `PillarGrid.astro` with container queries + `@supports` fallbacks.
- [ ] 8.3 `TrustChain.astro` primitive: `<ol role="list">`, stacks vertically `< 720 px`, no tab-stop on non-interactive `<li>`. `TrustChainTooltip.astro` per `DESIGN.md §4.13`.
- [ ] 8.4 Audit hero, standards row, bridge diagram at 360 / 480 / 720 / 1080 / 1440 px; fix overflow, clipping, tap-target violations (≥ 44 × 44 CSS px).
- [ ] 8.5 `tests/mobile-layout.spec.ts`: no horizontal overflow at 360 px, 44 px tap targets, single h1, skip-link first-tabbable, hamburger present `< 720 px`.
- [ ] 8.6 Migrate any legacy HTML mocks under `new-design/extracted/` onto tokens + primitives; delete `transform: scale()` blocks.

## Phase 9 — Structured data (site-structured-data)

- [ ] 9.1 `JsonLd.astro` helper with HTML-safe escape (no `set:html`; uses `JSON.stringify` + Astro's safe `<script type="application/ld+json">{json}</script>` pattern).
- [ ] 9.2 Sitewide Organization + WebSite JSON-LD emitted via `<JsonLd/>` wrapper into `BaseLayout.astro`'s `<slot name="json-ld">` (no direct BaseLayout edit). WebSite block does NOT include `potentialAction` SearchAction while `/search` is `noindex`.
- [ ] 9.3 Article + BreadcrumbList on `/writing/[slug]`. Article MUST include `publisher` resolved from sitewide Organization. Missing `cover` frontmatter emits a non-fatal build warning naming Top Stories ineligibility.
- [ ] 9.4 DefinedTerm / DefinedTermSet on `/standards`.
- [ ] 9.5 `scripts/validate-structured-data.mjs`: parse built HTML in `dist/`, validate JSON shape, required fields, absolute URLs, ISO dates, **assert no raw `</script>` substring inside any `<script type="application/ld+json">` block** (escape `<` as `&lt;` before interpolation in `JsonLd.astro`), **enforce aggregate ld+json size ≤ 8 KB uncompressed per route**, **`Article.publisher` matches sitewide Organization**.
- [ ] 9.6 (Optional) scheduled Google Rich Results check via GitHub Action.

## Phase 10 — Indexation (site-indexation)

- [ ] 10.0 Create `src/lib/indexation.ts` exporting `NOINDEX_ROUTES = ['/console', '/search', '/play', '/404'] as const`. Consumed by sitemap filter, BaseLayout `indexable` prop, robots.txt generator, and `validate-indexation.mjs` — single source.
- [ ] 10.1 Configure `@astrojs/sitemap` `filter` (consumes `NOINDEX_ROUTES`) AND `serialize` hook (binds `<lastmod>` to MDX `updated`/`published` frontmatter, NOT CI mtime) in `astro.config.mjs`.
- [ ] 10.2 `BaseLayout.astro` `indexable` prop; default true; set false on `/console`, `/search`, `/play`, `/404`.
- [ ] 10.3 Author `public/robots.txt`.
- [ ] 10.4 Confirm `astro.config.mjs` `trailingSlash: 'never'` (already set).
- [ ] 10.5 Author `public/_redirects` (or host equivalent) with the bridge 301 + any other moves.
- [ ] 10.6 `tests/redirects.spec.ts`: 301 on `/bridge` variants.
- [ ] 10.7 `scripts/validate-indexation.mjs`: meta-robots presence/absence per route (against `NOINDEX_ROUTES`); `<lastmod>` parity with frontmatter; `public/_redirects` destinations are same-origin (begin with `/`, no `://`, no `//`).
- [ ] 10.8 RSS `<link rel="alternate">` in `BaseLayout.astro`.

## Phase 11 — Branding (site-branding)

- [ ] 11.1 Generate favicon set (`favicon.ico`, `favicon.svg`, `apple-touch-icon.png`, `icon-192.png`, `icon-512.png`, optional `mask-icon.svg`).
- [ ] 11.2 `src/data/brand-colors.ts` constants — single source for theme-color and OG.
- [ ] 11.3 `scripts/generate-manifest.mjs` writes `public/site.webmanifest` from `brand-colors.ts`.
- [ ] 11.4 `theme-color` meta with `media="(prefers-color-scheme: dark)"` / `light`.
- [ ] 11.5 `scripts/generate-og-images.mjs` (satori-based) + fallback template; runs pre-build; per-slug OG outputs cached.
- [ ] 11.6 OG/Twitter meta in `BaseLayout.astro`.
- [ ] 11.7 Wordmark SVGs under `public/assets/brand/`.
- [ ] 11.8 `scripts/lint-brand.mjs` (or `rules/security/no-inline-wordmark.yaml`): forbid inline wordmark SVG outside `Footer.astro` and `Nav.astro`.

## Phase 12 — Quality gates

- [ ] 12.1 Playwright: cover smoke (h1 visible per route), bridge canonical sentence, 301 redirects, mobile layout, accessibility (`@axe-core/playwright`), trust-chain ARIA, theme toggle persistence.
- [ ] 12.2 **Edit `lighthouserc.json` first** (gates the rest of Phase 12): set `collect.url` to every marketing route plus `/writing/[slug]`; set assertions to `error` severity (not warn) at perf ≥ 0.9, a11y ≥ 0.95, SEO ≥ 0.95, best-practices ≥ 0.95; assert CWV: LCP ≤ 2500 ms, CLS ≤ 0.1, TBT ≤ 200 ms, INP ≤ 200 ms. Run desktop + mobile emulation. The current warn-only `/`-only config is a Critical defect — every other Phase 12 task assumes Lighthouse can fail CI.
- [ ] 12.3 CLS budget < 0.1 on every marketing route (Playwright measurement gate).
- [ ] 12.4 `@axe-core/playwright` sweep — zero serious violations.
- [ ] 12.5 SRI + CSP postbuild (`scripts/sri.mjs`, `scripts/csp.mjs`) still green; CSP hash list updated for the pre-paint theme script.
- [ ] 12.6 Lychee internal-only on PR; weekly full run.
- [ ] 12.7 All new linters wired into `npm run postbuild` and CI: `lint:tokens`, `lint:standards`, `lint:bridge`, `lint:taglines`, `lint:meta`, `lint:brand`, `lint:skip-link`, `validate:structured-data`, `validate:indexation`, `verify:font-metrics`.
- [ ] 12.8 ast-grep (`npm run lint:sg:ci`) passes — no new violations of `no-inner-html`, `no-set-html-directive`, `no-hardcoded-secrets`, `no-console-log-sensitive`.

## Phase 13 — Docs & announce

- [ ] 13.1 Update `README.md` with new routes, capability inventory, theme toggle, RSS feed.
- [ ] 13.2 Launch post under `/writing/` covering the redesign + theming + standards registry.
- [ ] 13.3 `openspec archive update-site-marketing-redesign` after deploy verification (use `--yes` for non-interactive).
- [ ] 13.4 File follow-up changes for: author public pages, changelog page, `/writing/tag/[tag]`, i18n, PWA installability.

## Parallelism notes

- Phases 2 + 4 can start in parallel; Phase 5 depends on both.
- Phases 6 and 7 are independent; both consume Phase 4.
- Phase 8 consumes 3 + 5.
- Phases 9, 10, 11 are independent after Phase 3, but share `BaseLayout.astro` — stage edits.

## Rollback

Revert PRs in reverse phase order. Per-phase revert is self-contained: each phase writes to its own capability dir and its own `src/` tree. The cross-cutting files (`BaseLayout.astro`, `theme.css`, `csp.mjs`) are touched by Phases 2/3/9/10/11 — if multiple revert, restore those files to the pre-Phase-2 baseline.
