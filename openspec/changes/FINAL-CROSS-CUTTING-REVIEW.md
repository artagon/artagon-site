# FINAL Cross-Cutting Review — Coherent Set

`openspec list` shows all three changes present (53/100/61 tasks, 0 done). `openspec validate <each> --strict` is GREEN for `adopt-design-md-format`, `update-site-marketing-redesign`, and `add-brand-assets-and-writing-pipeline`. This review focuses on coherence-of-the-set issues that no per-change review would surface.

## Cross-cutting findings

1. **[MEDIUM] Stale phase-number cross-reference: brand-assets references redesign "Phase 11.1".** `add-brand-assets-and-writing-pipeline/tasks.md:35` ("Replace the redesign's `site-branding` Phase 11.1 (favicon set hand-authoring)") and `proposal.md:18` ("Replaces the `site-branding` Phase 11.1 task") cite a numeric task in another change. Once the redesign archives, its `tasks.md` is moved to `openspec/changes/archive/<timestamp>-update-site-marketing-redesign/tasks.md` and the citation still resolves textually but the redesign's tasks are no longer the live spec — the live spec is `openspec/specs/site-branding/spec.md`. The reference is informational, not load-bearing, but it is brittle. **Remediation:** rephrase to "Replaces the redesign's hand-authored favicon task; the `site-branding` capability's Deterministic Favicon Generation requirement (this change) supersedes." Same for `design.md:72`.

2. **[MEDIUM] `adopt-design-md-format` design.md cites redesign "Phase 2.7-2.8" for `lint-tokens.mjs`.** `adopt-design-md-format/design.md:80` says "The `lint-tokens.mjs` script from `update-site-marketing-redesign` Phase 2.7-2.8 enforces 'no raw color literals…'". The redesign's `tasks.md:32` actually places `lint-tokens.mjs` at task **2.8**, not 2.7-2.8. Drift hazard if redesign tasks renumber. **Remediation:** drop the phase-number reference and cite by name: "the redesign's `Token Categories` requirement and its `scripts/lint-tokens.mjs`."

3. **[LOW] `update-site-marketing-redesign` design.md Open Questions still names brand gallery as "separate `add-brand-gallery` change".** `design.md:186-187` says the brand-icon gallery should "ship as a separate `add-brand-gallery` change. **Proposed:** separate change". The actual third change is named `add-brand-assets-and-writing-pipeline`, not `add-brand-gallery`. Once the redesign archives, the open question is moot, but the breadcrumb to the wrong change-id stays in archive history. **Remediation:** before redesign archive, edit the Open Question to "Adopted by `add-brand-assets-and-writing-pipeline`."

4. **[HIGH] Three different prerequisite-verification scripts with no consolidation.** `update-site-marketing-redesign/tasks.md:11` builds `verify:prerequisites` (checks refactor-styling); `adopt-design-md-format/tasks.md:12` builds `verify:design-prerequisites` (checks redesign uses old paths); `add-brand-assets-and-writing-pipeline/tasks.md:7` builds `verify:writing-prerequisites` (checks both prior changes archived). After all three land, `package.json` carries three near-duplicate scripts, three sources of truth for "is the openspec ordering satisfied?", with no umbrella runner. CI must call all three; one omission = silent ordering bypass. **Remediation:** add a Phase-0 task in the third change (it lands last) consolidating to `scripts/verify-openspec-prereqs.mjs --change <id>` with the three current scripts becoming thin wrappers OR the wrappers deleted in the third change's archive cleanup.

5. **[MEDIUM] Redesign Out-of-Scope says "Per-standard deep pages — registry supports deep linking… `/standards/[id]` pages are a follow-up."** The third change does not re-add `/standards/[id]`, so the deferral holds. However, `add-brand-assets-and-writing-pipeline/proposal.md:30` adds `<meta name="artagon:build-sha">` "on every marketing route" — the redesign's `site-indexation` requires noindex parity for `/console`, `/search`, `/play`, `/404`, and the third change extends that list with `/brand`. Build-sha meta should NOT emit on `/brand` (it's a tooling page, not user-facing). The spec at `site-writing-pipeline` Requirement "End-to-End Merge-to-Visible Latency" Scenario 2 says "the head contains `<meta name='artagon:build-sha' …>`" without scoping to non-noindex routes. **Remediation:** scope the build-sha meta to non-noindex routes, or explicitly state it emits on every route including noindex.

6. **[MEDIUM] No `pre-redesign` analog for the third change's rollback.** `update-site-marketing-redesign/tasks.md:12` (Phase 0.6) creates a `pre-redesign` git tag for multi-phase rollback. The third change's rollback section (`tasks.md:106-112`) is task-ordered but defines no tag. If a P95 latency regression surfaces a week post-deploy, "git tag pre-brand-assets" doesn't exist. **Remediation:** add Phase 0.5 to `add-brand-assets-and-writing-pipeline/tasks.md`: "tag `pre-brand-assets` once the redesign + design-md-format archives are confirmed."

7. **[MEDIUM] CI concurrency-group missing on `design-md-drift.yml`.** `adopt-design-md-format/tasks.md:30` adds `.github/workflows/design-md-drift.yml` (weekly cron) with no `concurrency:` declaration. `add-brand-assets-and-writing-pipeline/tasks.md:63` correctly sets `concurrency: { group: 'pages', cancel-in-progress: true }` on `content-redeploy.yml`. The drift cron commits to `openspec/.cache/design-md-spec.md` if regen produces a diff — if a deploy is mid-flight, both could push. **Remediation:** the drift workflow does not deploy (only opens a tracking issue per spec line 127), so a concurrency group is unnecessary if and only if the cron never pushes commits. Verify by adding to design-md-drift.yml spec: "the workflow MUST NOT push commits or open PRs; failure surfaces as a tracking issue only." If push behavior changes later, add concurrency.

8. **[LOW] Skill citation inconsistency.** `add-brand-assets-and-writing-pipeline/design.md:39` and `tasks.md:20` cite `javascript-typescript:typescript-advanced-types` for `as const satisfies BrandSvgs`. The redesign's `STANDARDS` registry (`src/data/standards.ts`, `tasks.md` Phase 6.1) uses the identical pattern but does not cite the skill — the third change's `design.md:60` even acknowledges this ("exact what the redesign's typed registry pattern uses for `STANDARDS`"). If the skill is renamed/unloaded, the third change's task acceptance signal is opaque. **Remediation:** rephrase to cite the pattern by name ("`as const satisfies` pattern preserves literal types") and treat the skill name as an informational hint, not part of the acceptance signal.

9. **[LOW] `adopt-design-md-format` Phase 8.4 creates an empty placeholder change.** `tasks.md:107` says "Create `openspec/changes/bump-design-md-to-beta/proposal.md` with only a `## Why` header." OpenSpec validate requires a populated proposal with `## What Changes` etc. — a stub will fail `openspec validate bump-design-md-to-beta --strict`. The cron only opens a tracking issue, so the stub is unnecessary anyway. **Remediation:** delete Phase 8.4; let the cron-opened tracking issue be the only forward record until upstream actually graduates.

10. **[LOW] `new-design/extracted/` artifact inventory has gaps.** Verified directory listing: `astro.config.mjs`, `package.json`, `.design-canvas.state.json` exist at `new-design/extracted/` root but are NOT in the `add-brand-assets-and-writing-pipeline/proposal.md` Appendix. `bun.lock`, `tsconfig.base.json`, `turbo.json` do NOT exist (so listing them as gaps in the user's request was a false positive). The `new-design/extracted/openspec/` subdirectory exists with its own `AGENTS.md`/`contributing.md`/`project.md`/`changes/`/`specs/` — the appendix mentions "openspec/changes/_ (4 in-flight proposals)" and "openspec/specs/_" but not the AGENTS/contributing/project triple. **Remediation:** add three appendix rows: `astro.config.mjs` (NOT adopted; superseded by repo-root `astro.config.mjs`); `package.json` (NOT adopted; superseded); `.design-canvas.state.json` (NOT adopted; tooling state); `openspec/{AGENTS,contributing,project}.md` (NOT adopted; superseded by repo-root copies).

11. **[MEDIUM] `add-brand-assets` MODIFIED requirement header text matches the redesign — verified.** `update-site-marketing-redesign/specs/site-content/spec.md:29` and `add-brand-assets-and-writing-pipeline/specs/site-content/spec.md:3` both use exactly `### Requirement: Per-route Frontmatter Contract`. The MODIFIED block will compose cleanly when the redesign archives first, then the third change archives. No remediation needed; document this match in the third change's Phase 10.5 acceptance (currently it just says "verify the merged form" — make the verification concrete: `grep "^### Requirement: Per-route Frontmatter Contract" openspec/specs/site-content/spec.md | wc -l` returns 1).

12. **[LOW] `adopt-design-md-format` `verify:design-prerequisites` semantics are inverted.** The script (Phase 0.6) "fails if (a) this change is archived AND `update-site-marketing-redesign`'s tasks reference `new-design/extracted/DESIGN.md` paths". But once `adopt-design-md-format` archives, the redesign change directory still has those references in `proposal.md`/`design.md`/`tasks.md` — those are historical authoring records, not consumed by build. The script will erroneously fail every CI run between `adopt-design-md-format` archive and `update-site-marketing-redesign` archive. **Remediation:** scope the path-grep to redesign's `tasks.md` only, AND only fail if the redesign is in flight (directory exists at `openspec/changes/update-site-marketing-redesign/`, not archived). Currently the spec at `tasks.md:12` does include "`update-site-marketing-redesign`'s tasks still reference" but the implementation hint is ambiguous between source files and live tasks.

## Archive-ordering canonical sequence

The single deterministic merge order:

1. `refactor-styling-architecture` (already 20/26 tasks done; archives first)
2. `adopt-design-md-format` (53 tasks; depends on #1 only loosely — Decision #8 says it MUST land before redesign)
3. `update-site-marketing-redesign` (100 tasks; verify:prerequisites enforces #1)
4. `add-brand-assets-and-writing-pipeline` (61 tasks; verify:writing-prerequisites enforces both #2 and #3)
5. (follow-up) `cleanup-new-design-extracted` (per third change's Phase 10.7) — deletes the `new-design/extracted/` directory once all references are removed.

Each script enforces:

- `verify:prerequisites` (#3): #1 archived OR ancestor of HEAD.
- `verify:design-prerequisites` (#2): #2 archived AND #3's `tasks.md` does not reference `new-design/extracted/DESIGN.md`.
- `verify:writing-prerequisites` (#4): both #2 AND #3 archived.

After consolidation per finding #4, a single `verify:openspec-prereqs.mjs --change <id>` enforces all three rules from one source.

## Verdict

**APPROVE-WITH-CHANGES.** Must-fix count: **3** (findings 4, 5, 12). Should-fix: **5** (findings 1, 2, 6, 7, 11). Optional: **4** (findings 3, 8, 9, 10).

The three changes are coherently authored, validate strict-green, and compose into a deterministic merge sequence. The MODIFIED `Per-route Frontmatter Contract` header text is byte-identical between the redesign and the third change, so archive composition is clean. The three prereq scripts are the most concerning artifact — they work but they're brittle and will accumulate. After fixes #4, #5, #12 land in their respective change's `tasks.md`, this set ships.

Confidence: 88%.
