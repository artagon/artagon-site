---
import Base from "../layouts/BaseLayout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import Tagline from "../components/Tagline.astro";
import MissionSection from "../components/MissionSection.astro";
import HeroChart from "../components/HeroChart.astro";
const page = {
  title: "Artagon — The provable identity cloud",
  description: "OIDC 2.1 + GNAP + passkeys + verifiable credentials + attestation + graph‑native authorization.",
  image: "/og/home.png"
};
---

<Base title={page.title} description={page.description} image={page.image}>
  <Fragment slot="header"><Header /></Fragment>

  <section class="hero-platform">
    <div class="container narrow grid">
      <div>
        <Tagline />
        <h1 class="gradient-text" style="font-size:clamp(2.2rem,1.4rem+3.2vw,3.4rem)">Artagon Identity Platform</h1>
        <p class="lead">
          OIDC 2.1, GNAP, Passkey sign‑in, W3C Verifiable Credentials (SD‑JWT, BBS+),
          device attestation, and graph‑native authorization—unified in one platform.
          <strong>The provable identity cloud.</strong>
        </p>
        <div class="chips" aria-label="Hardened profiles">
          <span class="chip">PAR</span><span class="chip">JAR/JARM</span><span class="chip">DPoP</span><span class="chip">RAR</span><span class="chip">mTLS</span>
        </div>
        <div class="actions">
          <a class="btn solid" href="/get-started">Get Started</a>
          <a class="btn ghost" href="/docs">Read the Docs</a>
          <a class="btn ghost" href="/play">Try the Playground</a>
        </div>
      </div>
      <HeroChart data={[12,18,14,22,28,19,31,27,34]} width={620} height={360} label="Verified claims & policy decisions trend" />
    </div>
  </section>

  <section class="section">
    <div class="container narrow grid-auto">
      <div class="col-4 card">
        <h3>Verifiable Everything</h3>
        <p>Move from asserted to proven identity: passkeys, VCs, device/app attestation.</p>
      </div>
      <div class="col-4 card">
        <h3>Zero‑Friction Security</h3>
        <p>Passkey‑primary UX; hardened OAuth/GNAP profiles by default.</p>
      </div>
      <div class="col-4 card">
        <h3>Privacy‑by‑Design</h3>
        <p>Selective disclosure (SD‑JWT/BBS+), unlinkable presentations, modern revocation.</p>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container narrow">
      <h2 class="gradient-text" style="margin-bottom:.6rem">How it works</h2>
      <ol class="howit">
        <li><strong>Passkey sign‑in</strong> (phishing‑resistant)</li>
        <li><strong>Device & app attestation</strong> (hardware‑backed where available)</li>
        <li><strong>Client proof‑of‑possession</strong> (DPoP/mTLS)</li>
        <li><strong>Verifiable Credential</strong> (OID4VCI/VP; SD‑JWT or BBS+)</li>
        <li><strong>Graph‑native authorization</strong> (Zanzibar + Cedar/OPA/XACML)</li>
      </ol>
    </div>
  </section>

  <section id="platform" class="section">
    <div class="container">
      <div class="grid cols-3">
        <div class="card">
          <div class="eyebrow">Unified OP / AS</div><h3>OIDC 2.1 + GNAP</h3><p>
            Hardened OAuth profiles (PAR · JAR/JARM · RAR · DPoP · mTLS), GNAP
            PoP & continuation, client key rotation.
          </p>
        </div>
        <div class="card">
          <div class="eyebrow">Verifiable Identity</div><h3>
            Selective disclosure
          </h3><p>
            Issue SD‑JWT & BBS+ VCs, verify via OID4VCI/VP, unlinkable
            presentations, revocation with StatusList2021 and Merkle proofs.
          </p>
        </div>
        <div class="card">
          <div class="eyebrow">Policy & Graph</div><h3>
            Zanzibar‑style checks
          </h3><p>
            Relationship graph as fast path with PDP overlays (XACML · Cedar ·
            OPA), obligations for redaction, masking, quotas, and consent.
          </p>
        </div>
      </div>
    </div>
  </section>

  <section id="how" class="section">
    <div class="container twocol">
      <div>
        <h2>How it works</h2>
        <div class="card" aria-label="Flow">
          <pre
            class="code"><code>1. Authenticate with passkey (FIDO2/WebAuthn)
2. Present minimal claims (OID4VP + SD‑JWT/BBS+)
3. Graph check + policy overlay → DPoP/mTLS‑bound token</code></pre>
        </div>
      </div>
      <div class="grid">
        <div class="card">
          <div class="eyebrow">Attestation</div><strong
            >Android Play Integrity · Apple App Attest · WebAuthn</strong
          ><br />Evidence-bound clients with rotation and per-device DPoP
          binding.
        </div>
        <div class="card">
          <div class="eyebrow">Proofing</div><strong
            >NIST 800‑63 / eIDAS mapping</strong
          ><br />Pluggable doc/biometric KYC → Proofing VC for onboarding &
          step‑up.
        </div>
      </div>
    </div>
  </section>

  <MissionSection />

  <section id="developers" class="section">
    <div class="container twocol">
      <div>
        <h2>For developers</h2>
        <p>
          SDKs for Java · Rust · JS/TS · Go · Swift, GraphQL + REST APIs, and a
          conformance harness. Spin up a sandbox tenant in minutes and test
          OIDC, GNAP, and VC flows with real tokens.
        </p>
        <div class="actions">
          <a class="btn" href="https://docs.artagon.com">Browse APIs</a>
          <a class="btn" href="https://play.artagon.com">Open Playground</a>
          <a class="btn" href="https://github.com/artagon" rel="noopener"
            >GitHub</a
          >
        </div>
      </div>
      <div class="card" aria-label="Quickstart">
        <div class="eyebrow">Quickstart (OIDC + DPoP)</div>
        <pre
          class="code"><code>curl -X POST https://id.artagon.com/oauth2/token \
 -H "DPoP: &lt;jwt&gt;" -H "Content-Type: application/x-www-form-urlencoded" \
 -d "grant_type=authorization_code&amp;code=..."</code></pre>
        <div class="discrete">
          Use our SDKs to generate DPoP and handle key rotation automatically.
        </div>
      </div>
    </div>
  </section>

  <section id="faq" class="section">
    <div class="container">
      <h2>Frequently asked questions</h2>
      <details>
        <summary>What is the Artagon Identity Platform?</summary><p>
          Artagon combines an OIDC 2.1/OpenID Provider, a GNAP Authorization
          Server, verifiable credentials, device/app attestation, and
          Zanzibar‑style policy in one platform.
        </p>
      </details>
      <details>
        <summary>Does Artagon support OIDC 2.1 and GNAP?</summary><p>
          Yes—OIDC 2.1 with hardened OAuth (PAR · JAR/JARM · RAR · DPoP · mTLS)
          and GNAP with proof‑of‑possession and continuation handles.
        </p>
      </details>
      <details>
        <summary>Do you support passkeys and verifiable credentials?</summary><p
        >
          Yes—passkey/WebAuthn is primary auth, and we issue/verify SD‑JWT &
          BBS+ VCs via OID4VCI/OID4VP.
        </p>
      </details>
      <details>
        <summary>How does authorization work?</summary><p>
          A Zanzibar graph powers fast relationship checks while policies in
          XACML, Cedar, or OPA apply fine‑grained controls and obligations.
        </p>
      </details>
    </div>
  </section>

  <section class="section cta-section">
    <div class="container" style="text-align:center">
      <h2 style="margin-bottom:1.5rem">Ready to Get Started?</h2>
      <p style="color:var(--muted);max-width:700px;margin:0 auto 2rem">
        Start building with Artagon's unified identity platform. Deploy passkey authentication,
        verifiable credentials, and graph-native authorization in minutes.
      </p>
      <a class="cta cta-large" href="https://console.artagon.com">Get Started</a>
    </div>
  </section>

  <Fragment slot="footer"><Footer /></Fragment>
</Base>
